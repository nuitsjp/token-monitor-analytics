# Copy to /etc/token-monitor-analytics/analytics.env, root:root 0600.
# Generate independent random values. This is a systemd EnvironmentFile, not a shell script.
TMA_INGEST_TOKEN=REPLACE_WITH_64_HEX_CHARACTERS
TMA_VIEWER_USER=analytics
TMA_VIEWER_PASSWORD=REPLACE_WITH_ANOTHER_RANDOM_VALUE
