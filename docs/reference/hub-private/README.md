# Private Hub の実データ資料

アーキテクチャ設計時に、Hub が実際に返すデータ構造・識別子・利用枠・履歴を確認するための資料。

- 取得元: https://private.token-monitor-hub-private.workers.dev
- 取得日時: 2026-09-12 01:57:13–01:57:14 UTC（日本時間 10:57:13–10:57:14）
- 保存先: [2026-09-12T01-57-13-988Z](2026-09-12T01-57-13-988Z/manifest.json)
- 認証: リクエストの Authorization ヘッダーで指定。シークレットとリクエストヘッダーは保存していない。

## 保存内容

各ファイルはレスポンス本文をそのまま UTF-8 で保存したもの。個人の実データとして、アカウント識別情報、端末名、プロジェクト情報なども含む。匿名化したテストデータではない。

| ファイル | GET エンドポイント | 結果・内容 |
| --- | --- | --- |
| `health.json` | `/api/health` | 200。稼働状態と Hub のビルド識別情報。 |
| `stats.json` | `/api/stats` | 200。集約した利用実績・利用枠。 |
| `devices.json` | `/api/devices` | 200。2 台分の端末別実績・利用枠・保持履歴。 |
| `history.json` | `/api/history` | 200。Hub が保持する集約履歴。 |
| `subscriptions.json` | `/api/subscriptions` | 200。Hub の契約設定。 |
| `public-stats.json` | `/api/public/stats` | 404。取得不能だった応答も保存。 |
| `stream-snapshot.sse` | `/api/stats/stream` | 200。接続時の最初の snapshot イベントを保存して切断。 |
| `manifest.json` | — | 取得元、各リクエストの開始・終了日時、HTTP 状態、本文バイト数、SHA-256。 |

## 範囲と制約

取得経路は、上流ソース `upstream/token-monitor/worker/src/index.js`（コミット `2f60827e3028d283969dd74cde5b3f5664220442`）にある参照用 API を確認した。これらの GET にページング指定はなく、応答全体を保存している。更新・削除 API は実行していない。

これは取得時点で参照 API が公開するデータであり、Hub の内部ストレージ全体のバックアップではない。Hub が保持していない履歴や、過去の利用枠の観測を復元したものではない。SSE は初回イベントだけで、将来の更新イベントを継続収集していない。

各 API は独立に並行取得しているため、単一トランザクションのスナップショットではない。ファイル間の値が厳密に同時点であるとは扱わない。また、取得日時と各端末・利用枠の更新日時は異なる。増分の推定に使う際は、データ内の更新日時・状態・リセット時刻を確認すること。

この一回の取得だけでは、利用許容量の推定に必要な異なる時点の二つの有効な観測がそろったとは扱わない。
