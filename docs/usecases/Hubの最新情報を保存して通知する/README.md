# Hubの最新情報を保存して通知する

## 主アクター

利用者。起動後の受信・保存・通知は自動で行います。

## 目的

Hubの最新情報をローカルで利用可能にし、閲覧側へ更新を通知します。

## 前提

2つのHubについて、安定したID・表示名・URL・認証トークンを含む接続設定ファイルが用意されています。

## 保存対象

HubのID・表示名、Hubが配信する利用量、上限、端末状態等の最新状態と受信時刻。URL、認証トークン、受信イベントの履歴は保存しません。

## 共通の受け入れ条件

- 初回状態・全体更新・鮮度更新の保存成功後に、保存済み状態と一致する閲覧用データが通知されます。
- 保存確定前、不正通知、保存失敗、heartbeatでは、データ更新を通知しません。
- 閲覧側の接続時には、その時点の最新保存値を通知し、以後の更新へ取りこぼしなく接続します。
- 複数の閲覧側へ同じ最新状態を提供できます。通知は最新状態の共有を目的とし、途中の全イベントの再送は保証しません。
- 閲覧側が未接続または切断されても、Hubからの受信・保存は継続します。通知失敗によって保存済みデータを取り消しません。
- 一方の認証失敗・通信断・不正通知・保存失敗でも、もう一方の受信とWebサーバーは継続します。通信断からの当該Hubの再接続は[Hub切断時に再接続する](scenarios/Hub切断時に再接続する.md)で扱います。
- 同じ情報を再受信しても利用量が二重加算されません。
- 不正な通知や保存失敗では、最後に保存できた有効な状態を維持します。
- URLと認証トークンをDB、受信データの保存内容、ログ、閲覧側への通知に含めません。設定ファイルはアプリケーションから更新しません。

## シナリオ

- [Hubの最新情報を保存して通知する](scenarios/Hubの最新情報を保存して通知する.md)
- [Hub切断時に再接続する](scenarios/Hub切断時に再接続する.md)

## 実現パターン

[UCP-1](../../design/UCP-1.md) を適用します。通知処理はアプリケーション単位で共有し、受信処理は特定の画面やコンポーネントを参照しません。保存設計は [データ設計](../../design/data.md) を参照します。テーブル変更はありません。
