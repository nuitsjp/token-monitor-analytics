# UC-2. 利用状況を閲覧する

- 主アクター: 利用者。
- 目的: 登録Hubとデバイスの最新状態を、ダッシュボードのひとつの画面で確認します。
- 前提: アプリケーションが起動し、Hubが接続設定へ登録されています。
- データ取得: 画面を開いた時に一度取得します。自動更新と手動更新は行いません。

## 主成功系列

UC-2-M / UI確認: 必要。A案（Workspace）をトップ画面に採用し、Hub・デバイスの配置、未受信状態、画面全体の読みやすさを確認します。

| 手順 | アクター         | 操作・処理                                                                  |
| ---- | ---------------- | --------------------------------------------------------------------------- |
| 1    | 利用者           | アプリケーションのトップ画面を開く                                          |
| 2    | 画面             | 利用状況取得APIを一度呼び出す                                               |
| 3    | アプリケーション | 現在登録されているすべてのHubについてSQLiteの最新状態を読み取る             |
| 4    | アプリケーション | Hub名、受信状態、デバイス情報、期間別トークン数・推定コスト、データ更新日時を返す |
| 5    | 画面             | 登録Hubとデバイスを区別してダッシュボードのHub・デバイス欄へ表示する        |

未受信のHubは「まだ情報を受信していません」と表示します。取得に失敗した場合はHub・デバイス欄に「Hub・デバイスを取得できませんでした」と表示し、保存済みデータと接続設定を変更しません。

## 受け入れ条件

- 現在の接続設定に登録されているすべてのHubを区別して表示し、Hub数を固定しません。
- 過去に設定から外れ、DBだけに残っているHubは表示しません。
- Hub別トークン使用量と推定コストを、日本語の項目名・合計値・構成比の積み上げ棒・色付きの一覧で表示します。構成比はマウスオーバーまたはフォーカスで確認できます。Hub名のすぐ右へ受信デバイスのホスト名を小さく並べます。右端のトークン数・推定コストを優先してホスト名を省略し、マウスオーバーまたはフォーカスで全文を表示します。
- TODAY・MONTH・TOTALは保存済みの `stats.periods.today`・`month`・`allTime` に対応します。Hub欄の数値とグラフを期間に合わせて切り替えます。合計が0の指標は空の棒を表示し、未受信Hubは集計へ加えません。
- 受信済みHub数、登録Hub数、受信済みデバイス数を表示します。未受信Hubはデバイス数へ加えません。
- Hub行には台数・更新日時・Hub間の仕切り線を表示しません。
- Hub欄の数値はSQLiteに保存された最新状態と一致します。
- 画面内のトークン数は、合計・一覧・グラフの目盛りを含め、単位による省略をせずカンマ区切りの整数で表示します。
- 未受信のHubには、Hub名と未受信メッセージを表示します。
- APIは接続先URLや認証トークンを返しません。
- 画面表示によってSQLiteの内容は更新されません。
- 画面を再読み込みすると、その時点でSQLiteに保存されている最新値を表示します。
- 自動更新と手動更新は対象外です。Hub欄以外のトークン、コスト、アクティビティ、ツール、モデル、利用上限、グラフは将来の実装位置を示す固定サンプルで、SQLiteやAPIから取得しません。

## 実現パターンと現在地

[UCP-2](../architecture.md#ucp-2) を適用します。新しいテーブルやカラムは追加しません。

段階1の本文とUCP-2は確認済みです。段階2のモックを作成し、Chromeで事前確認済みです。A案の採用と初期実装範囲について段階3の動作合意を得ています。

段階5の完成系監査を承認済みです。段階6のE2Eと実環境検証を実施しています。

- 完成系監査の承認（2026-09-21、リポジトリ所有者、UC-2-M）: 利用者が修正したデザインを採用し、次段階への進行を承認しました。確認時に見つかった狭い画面の数値のはみ出しは、列数の調整で修正しました。

  > デザインを修正しました。確認の上、次へ進めてください。

- 完成系監査中のデザイン改善（2026-09-21、リポジトリ所有者）: 初見レビューの提案をすべて採用します。色・余白・文字の正本を `frontend/src/app/theme.ts` に集約し、Hub・デバイス欄を1段目右（利用トレンドの隣）へ移し、利用枠を2段目へ移します。左ナビは本文順（トレンド、Hub・デバイス、ツール、モデル、利用枠、アクティビティ）に揃え、スクロール位置に追従します。「LIVE」は「保存値」に変え、Hub・デバイス欄と左ナビに取得時刻を表示します。ツールとモデルは同じランキングバー、利用枠は残量20%未満で注意色・10%未満で危険色、ヒートマップは正方形セルと月・曜日ラベル・日付ツールチップにします。補助テキストはコントラスト比4.5:1以上、最小文字は12pxとします。固定サンプルと実データの数値矛盾、API失敗時の0表示は固定サンプル段階のため対象外です。

  > あなたの提案をすべて受け入れます。
  > すべて自律的に対応し、完全なブロッキングが発生するまで自律的に遂行してください。

- 完成系監査中のカードデザイン合意（2026-09-21、リポジトリ所有者）: 提示コミット `208dbfc` の [カード比較](../../design-samples/hub-cards.html) のA案「合計値＋積み上げ棒」を採用します。

  > OKです。Aを採用します

- 完成系監査中の表示修正（2026-09-21、リポジトリ所有者）: Hub欄へ期間別トークン数・推定コストを追加し、ホスト名は使用量を優先して省略表示します。

  > フォントサイズはOK。表示内容はモデルのように棒グラフ化してください。
  > ハブ名のすぐ右に、小さくホスト名を出しますが、右側のトークン使用量などの方を優先し、表示しきれない場合は、省略してマウスオーバーで表示します。

- 段階3のA案採用と実装範囲（2026-09-21、リポジトリ所有者）: 提示コミット `3d75ab6` のA案（Workspace）を採用します。初期実装ではHub・デバイスだけをSQLiteの保存済み状態へ接続し、他のパーツは固定サンプルとして表示します。

  > Aにします。ただ、初期実装はHub・デバイスだけ動作するように実装して最後の段階まで完了させ、各パーツはインクリメンタルに実装していきます。
  > それ以外の部分はハードコード実装でOKです。

- 段階3の変更（2026-09-20、リポジトリ所有者）: Hub別カードの上にHubの合計値を追加します。

  > Hub別のカードの上に、Hubの合計値の表示を追加してください

- 段階3の表示調整（2026-09-20、リポジトリ所有者）: 見出しと合計値を統合し、補足文と「全Hub合計」の見出しを削除して全体をコンパクトにします。

  > この部分は統合してください。

- 段階3の表示調整（2026-09-21、リポジトリ所有者）: 製品名だけを集計領域の外に表示します。

  > Token Monitor Analytics だけ外にだしてください

- 合意記録（2026-09-20、判断者: リポジトリ所有者）:
  - UC-2-M / 提示コミット: `e550c5d` / 論点と回答: 登録Hubをすべて表示し、受信データの更新日時をローカル日時で表示し、取得失敗は画面全体へ表示する内容で合意。提示コミットは確認後に保存した本文を特定します。
    > OKです。それで進めてください
