{
  "name": "token-monitor-analytics-web",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node --experimental-strip-types runtime/server.mjs --config configs/demo.json",
    "start": "node --experimental-strip-types runtime/server.mjs --config config.local.json",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test test/*.test.mjs",
    "backup": "node --experimental-strip-types runtime/backup.mjs"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
