# 用語定義（CONTEXT）

複数の Token Monitor Hub から収集した利用実績と利用枠の観測に基づき、定額制 AI サブスクリプションの利用許容量を推定・記録する領域の用語を定義する。

## 用語一覧

**Hub**:
複数端末の利用実績と利用枠の観測を集約し、本システム（Analytics）に提供する接続先。接続先ごとに独立した情報源として扱う。

**端末**:
AI ツールの利用実績やアカウントの利用枠を収集し、Hub へ報告する実行環境。1 台の端末で複数のツールやアカウントを扱う場合がある。

**ツール**:
Claude Code や Codex など、利用実績を分類する AI アプリケーション。同一ツールの利用であっても、契約が同一とは限らない。

**プロバイダー**:
アカウントの利用枠を提供するサービス事業者。同一プロバイダー内に複数のアカウントや契約が存在し得る。

**アカウント**:
プロバイダー上で利用者や組織を識別する単位。端末やツールとは独立して、契約や利用枠の観測対象となる。

**契約**:
利用許容量の推定対象となる、アカウントに紐づく定額制サービスの利用契約。プラン名が同一でも別契約の場合がある。

**利用枠**:
契約内で設定される利用制限の個別の割り当て単位（例: 同一契約内の短時間枠や週次枠）。
*注記: 利用許容量と混同しないこと。*

**対象期間**:
単一の利用枠において、消費量を同一基準で比較する期間。日別・月別の利用実績集計期間とは必ずしも一致しない。

**利用実績**:
Hub から報告されるトークン消費量や API 換算額などの実績集計値。契約の実際の請求額や利用許容量そのものではない。

**API 換算額**:
消費したモデルやトークン量を従量課金の API 料金に換算した米ドル建て金額。定額契約の月額料金等とは異なる。

**消費率**:
利用枠に対する消費済みの割合（パーセンテージ）。消費率が 20% から 25% に増加した場合、その差分は 5 パーセントポイントとなる。
*注記: 残量や残り率とは区別する。*

**観測**:
特定時点で取得された利用実績または利用枠の状態。同一ペイロードで届いた実績と利用枠であっても、測定時点が同一とは限らない。

**増分**:
同一の契約・利用枠・対象期間に属する、比較可能な 2 点の観測間における API 換算額や消費率の増加量。

**利用許容量**:
利用枠を 100% 消費した場合に相当する米ドル換算の推定総量。公式の上限値や保証値ではなく、実測値に基づく参考値である。

**推定**:
対応する API 換算額と消費率の実測増分から、利用許容量を算出すること。

**補完**:
受信できなかった過去の利用実績を、Hub が保持する履歴データから取得して蓄積すること。未観測の利用枠や推定値を推測で補うことは含まない。

**リセット**:
利用枠の消費基準が更新され、以前の観測と同一の対象期間として比較できなくなる事象。利用実績の日付変更や期限付き残高の失効とは区別する。

**推定不可**:
必要な観測データの不足や対応関係の不整合により、利用許容量を算出できない状態。利用実績がゼロであることや、過去に算出した推定結果の不存在を意味するものではない。
