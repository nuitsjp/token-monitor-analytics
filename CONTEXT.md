# 用語定義 (CONTEXT)

Token Monitor Analytics で使用するドメイン用語の定義です。

## 情報源と利用主体

- **Hub**: 端末の利用実績や利用枠の観測データを集約し、本システムへ提供する外部サーバー。
- **端末**: AI ツールの利用実績や利用枠を収集し、Hub へ報告するクライアント環境。
- **ツール**: 利用実績の分類単位となる AI アプリケーション（Claude Code、Codex 等）。
- **プロバイダー**: アカウントや利用枠を提供するサービス事業者（Anthropic、OpenAI 等）。
- **アカウント**: プロバイダー上で利用者や組織を識別する単位。
- **契約**: アカウントに紐づく定額制サービスの利用契約。共通設計では、プロバイダーと観測上の契約IDを組み合わせてHubをまたいだ同一性を判定し、Hubの違いだけでは別契約にしない。Grokは既合意の正規化メール由来キーを使い、認証情報の更新だけでは契約を分けない。
- **観測上の契約ID**: Hubをまたぐ観測の対応付けに使う識別子。原則として同じ `provider + accountKey` を用いるが、公式の契約IDであることや不変性は保証しない。Grokは正規化した `accountEmail` 由来の既合意キーを用いる。
- **共通契約**: 複数Hubや端末から報告されても、同じプロバイダーと観測上の契約IDにより一つの契約として対応付けられる契約。プランは同一性ではなく契約の属性である。
- **Hub内契約**: schema 4 以前の方式で、Hubをまたぐ観測上の対応付けに必要な情報がない場合にHubの範囲でのみ管理していた契約。現在の共通設計では識別子不足時にHub内契約を代替生成せず、推定不可として扱うため、過去の方式を説明する歴史用語である。
- **プラン**: 同じサービスにおける契約上の利用条件の種別。
- **基準プラン**: 同じサービス・同じ種類の利用枠について、利用上限倍率を1とする基準のプラン。
- **利用上限倍率**: 基準プランの利用許容量に対する、別プランの利用許容量の比。利用枠の種類ごとに定まり、料金の比とは異なる。

## 利用枠と観測

- **利用枠**: 契約内で設定される個別の利用制限単位（短時間枠、週次枠等）。
- **枠キー**: 利用枠を観測間で対応付ける属性の組。枠の種類・期間長・任意の識別子・追加枠かどうか・指標を基本とし、識別子がない場合は明示ラベルも用いる。
- **対象期間**: 単一の利用枠の消費量を同一基準で比較できる有効期間（日次・月次の実績集計期間とは独立）。
- **リセット**: 利用枠の消費基準が更新され、以前の観測値と同一期間として比較できなくなること。
- **利用実績**: Hub から報告されるトークン消費量や API 換算額などの集計値（実際の請求額とは異なる参考値）。
- **API 換算額**: 消費トークン量を従量課金 API 料金に換算した米ドル建て金額。
- **消費率**: 利用枠に対する消費済みの割合（%）。取得項目が複数ある場合の採用順と有効範囲は機能仕様で定め、画面の残量率とは区別する。
- **観測**: 特定時点で取得された利用実績または利用枠の状態データ。
- **増分**: 同じ推定対象について比較可能な2つの観測間の、API 換算額または消費率の増加量。
- **共有利用額**: 複数契約の利用分を含む、契約別には分離されていない API 換算額。
- **利用実績範囲**: 同じ利用実績を重複計上しないための収集範囲。アカウント全体と端末単位を区別する。
- **論理利用額ソース**: 同じ利用実績を一度だけ数える観測上の単位。利用実績範囲に応じて、同一契約集合の複数端末報告を一つへまとめる場合と、端末ごとに分ける場合がある。
  _Avoid_: 利用額ソース（物理的な端末報告を指す場合）

## 推定と履歴

- **利用許容量**: 利用枠を 100% 消費した場合に相当する、米ドル換算の推定総量（実測に基づく参考値）。
- **推定**: 同一対象期間におけるツール全体の API 換算額と各利用枠の消費率の実測増分比から、利用枠ごとの利用許容量を算出する処理。利用額を枠別課金へ按分せず、枠別の推定値を合算しない。
- **推定対象**: 共通契約で対応付けられた論理利用額ソース、契約、利用枠を、同じ比較条件で扱う集合。
- **部分推定**: 取得不能な端末を除外し、残る論理利用額ソースと契約の観測だけから算出した参考値。
- **推定不可**: データの不足や不整合により、利用許容量を算出できない状態。
- **補完**: Hub の保持履歴から過去の未取得実績を取得して蓄積すること。
