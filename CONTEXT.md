# Token Monitor Analytics

Token Monitor Hub の観測データと、利用者が AI サービス上で契約するアカウントを結び付け、アカウント単位の利用価値を分析する文脈を定義する。

## Language

### 識別とカタログ

**Hub**:
Token Monitor の観測データを集約し、Analytics に提供する単位。

**Hub 識別子**:
URL、資格情報、収集担当から独立して、同じ Hub を継続して表す不変の識別子。

**サービス提供者**:
OpenAI や Anthropic など、サービスと契約プランを提供する主体。

**サービス**:
Codex や Claude Code など、利用状況を分析し、契約プランを定義する対象。

**参照カタログ版**:
Analytics が組み込み参照データの内容を識別する版。同じサービスの同一性やプラン版を表すものではない。
_Avoid_: サービス版、provider 版

**生利用額サービス識別子**:
Hub が利用額の内訳に付けて報告する未同定のサービス識別子。正式なサービスとの対応が確認されるまでは、正式なサービスとはみなさない。

**生利用枠サービス識別子**:
Hub が利用枠に付けて報告する未同定のサービス識別子。生利用額サービス識別子とは別に、正式なサービスとの対応を確認する。

**サービス・プラン同定候補**:
元観測に現れた生利用枠サービス識別子と生報告プラン名の組を、正式なサービスとプランへ対応付けるための候補。未確認、確認済み、対象外の状態を持つ。

**Hub アカウント**:
特定の Hub がアカウント識別子や表示情報で報告する観測上のアカウント。別の Hub が報告するアカウントとの同一性は、自動では確定しない。

**Grok 主体識別子**:
Grok の認証発行者の中で、個人又は Team の利用主体を資格情報の更新から独立して表す識別子。個人と Team は別の主体として扱う。

**資格情報指紋**:
アクセストークンなど一つの資格情報を識別する値。資格情報の更新で変化するため、Hub アカウント又は論理アカウントの同一性には使用しない。

**論理アカウント**:
同じサービス上の一つの利用契約を、複数 Hub の観測をまたいで表す分析単位。利用を止めた後も、過去の同一性と関連付けを保ったままアーカイブできる。

**プラン**:
サービスに属する正式な契約上の利用種別。

**観測済み利用資格**:
Hub が特定の Hub アカウントについて報告したプラン又は利用資格の証拠。正式なプランと一意に対応するまでは、プランそのものとはみなさない。
_Avoid_: 推定プラン、自動作成プラン

**プラン版**:
プランの属性が特定の期間に有効であることを表す版。論理アカウントのプラン履歴と組み合わせて、観測時点の条件を決める。

**プラン履歴**:
論理アカウントに適用されたプラン版を、有効期間付きで記録した履歴。

**基準プラン**:
同じサービス内で、利用上限倍率の基準として定めるプラン。

### 観測と関連付け

**原 JSON スナップショット**:
Hub API から一回の取得で得た応答の原形。正規化した観測や派生計算結果とは別に、履歴上の根拠として扱う。

**元観測**:
原 JSON スナップショットから、出典を失わずに取り出した観測事実。設定変更や派生計算結果とは区別する。

**取得元期間観測**:
Hub が期間キー、期間終了時刻、取得元タイムゾーン付きで報告した、当日または当月の暦期間に対する利用実績。累積値の差分ではない。
_Avoid_: 日次差分、暦日差分

**観測間増分**:
同じ利用額ソースの隣接する累積観測の差。任意の観測間で増えた量であり、暦期間の確定値ではない。
_Avoid_: 日次実績、当月実績

**利用額ソース**:
特定の Hub 端末レコードと生利用額サービス識別子から得られる、累積 API 換算利用額などの観測元。論理アカウントの識別情報は持たない。

**利用枠ソース**:
特定の Hub 端末レコード、Hub アカウント、生利用枠サービス識別子、利用枠ウィンドウから得られる利用率の観測元。

**観測ウィンドウ照合キー**:
同じ利用枠ウィンドウの観測を時系列に照合するための、種別、指標、表示名からなる正規化済みの組。Hub が発行する永続的な利用枠 ID ではない。

**利用枠定義**:
サービスをまたがない範囲で、同じ意味と周期を持つ利用枠を表す正式な定義。複数 Hub や複数論理アカウントの利用枠系列に共通のプラン利用上限を適用する単位となる。

**アカウント利用枠系列**:
同じ論理アカウントと利用枠定義に属する利用率観測を、取得元 Hub にかかわらず時刻順にまとめた系列。確認済みの名称変更前後も一つの系列として扱える。

**利用枠観測不整合**:
同じアカウント利用枠系列の同一観測時刻に、異なる利用率またはリセット日時が報告された状態。

**利用額関連付け**:
利用額ソースと論理アカウントの有効期間付きの対応。利用額をアカウント別に確定配分したことは意味しない。

**利用枠関連付け**:
利用枠ソースと論理アカウントおよび利用枠定義の有効期間付きの対応。

**対応観測**:
一つの基準時刻に対して、許容時刻差内で選ばれた元観測の対応。補間して作った値ではない。

**推定観測点**:
一つの利用上限推定に必要な全論理アカウントの利用率と、全利用額ソースの累積 API 換算利用額が、同じ基準時刻についてそろった観測の組。

**時刻差**:
対応観測の基準時刻と、実際の観測時刻との差。取得処理の完了時刻との差ではない。

**収集端末**:
Token Monitor がローカル利用ログを収集する一台の物理端末。

**Hub 端末レコード**:
特定の Hub が管理する端末データ。別の Hub にある同名または同一識別子のレコードとの同一性は、自動では確定しない。

### 周期と推定

**利用周期**:
一つの利用枠がリセットされてから次にリセットされるまでの期間。暦週や暦月とは一致しない場合がある。

**リセットイベント**:
利用枠の累積利用率が新しい利用周期へ切り替わったことを示す事象。

**計算区間**:
同じ推定条件を保ち、リセット、プラン変更、関連付け変更、観測系列の断絶などの境界をまたがない半開の時間区間。

**カレント計算区間**:
現在有効な推定条件について、直近の観測を含む計算区間。必要な観測が不足している場合もある。

**最新有効計算区間**:
現在有効な推定条件について、推定済みを得られた最も新しい過去の計算区間。

**観測系列の断絶**:
累積値の減少などが既知のリセットや設定変更では説明できず、同じ差分系列を継続できない境界。

**プラン利用上限**:
同じプラン版と利用枠定義の組に共通する、一利用周期当たりの利用上限。Hub の観測から API 換算額として推定する。

**月間換算推定利用上限**:
一利用周期当たりのプラン利用上限を、月額標準シート単価と比較できる月次単位へ換算した値。月間の実利用額や請求額ではない。

**利用上限倍率**:
同じサービスにある基準プランのプラン利用上限に対する相対比。プラン版と利用枠定義の組に属し、価格比とは異なる。

**推定済み**:
必要十分な観測と有効な差分から、正のプラン利用上限が一意に求められた推定。計算可能な推定であることを意味し、外部の正解値に対して検証済みであることを意味しない。

**推定対象外**:
利用枠の種別や単位が、利用上限推定へ入力できる範囲に含まれない状態。元観測や利用実績分析から除外することは意味しない。

**未算出**:
対応 API 契約、関連付け、完全性確認、プランまたは倍率などの前提が不足し、方程式系を評価できない状態。

**観測不足**:
推定観測点または有効差分行の数が、未知数を求めるための必要数に達していない状態。

**識別不能**:
行数は必要数に達しているが独立した変化が不足するか、利用額の信号がなく、対象のプラン利用上限を一意に定められない状態。

**モデル不適合**:
観測数と階数が足りても有限かつ正の解を得られない状態。

### 利用実績と価値

**共有観測利用額**:
複数の論理アカウントに関係する利用額ソースで観測された API 換算利用額。全体では一度だけ集計し、推定前にアカウント別実績へ配分しない。

**共有利用実績**:
複数の論理アカウントに関係する利用額ソースのトークン数、API 換算利用額、モデル内訳。全体では一度だけ集計し、アカウント別へ按分しない。

**推定アカウント寄与額**:
利用率差分と推定したプラン利用上限から求める、論理アカウントごとの API 換算利用額への寄与。共有観測利用額そのものではない。

**利用実績分析**:
指定期間内のトークン数、API 換算利用額、モデル内訳、残高などを、それぞれの元の単位で扱う分析領域。

**標準シート単価**:
サービス提供者が公表する、プラン一シート当たりの USD 建て月額標準価格。実際の請求額や契約固有価格ではない。

**標準価格推定最大価値倍率**:
月間換算推定利用上限を月額標準シート単価で除した、標準価格に対する推定 API 換算価値の比率。

### データ管理

**派生計算結果**:
元観測、関連付け、プラン履歴などから導出した分析結果。根拠と計算論理の版をたどれる状態で保持する。

**設定変更監査履歴**:
ユーザーによる設定変更を、変更日時、対象、変更内容とともに記録した履歴。

**明示パージ**:
ユーザーが対象を確認したうえで、原 JSON スナップショットとそれを根拠とする派生計算結果を不可逆に削除する操作。

**ローカル観測正本**:
各端末が収集した元観測について、原形と履歴の根拠となるローカル側のデータ。

**ローカルバックアップ**:
ローカルデータベースで管理する非秘密データを、完全性と互換性を検証できる自己完結した成果物へ保存したもの。

**クラウド複製**:
複数端末のローカル観測正本を統合する、将来拡張の二次的な複製データ。ローカル観測正本そのものではない。

**表示タイムゾーン**:
UTC で保持した時刻を、画面上の暦日、暦週、暦月へ変換するために利用者が選ぶタイムゾーン。

**取得元ローカル日付**:
取得元がローカル日付で既に集計したデータの日付。取得元タイムゾーンが分からない限り、UTC の日付へ再配分しない。
