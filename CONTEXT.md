# 用語定義

Token Monitor Analytics で使用するドメイン用語の定義。

## 用語一覧

**Hub**  
端末の利用実績や利用枠の観測を集約し、本システムへ提供する接続先。Hub ごとに独立した情報源として扱う。

**端末**  
AI ツールの利用実績や利用枠を収集し、Hub へ報告するクライアント環境。1 台で複数のツールやアカウントを扱う場合がある。

**ツール**  
Claude Code や Codex など、利用実績の分類単位となる AI アプリケーション。

**プロバイダー**  
アカウントや利用枠を提供するサービス事業者（Anthropic、OpenAI など）。

**アカウント**  
プロバイダー上で利用者や組織を識別する単位。

**契約**  
アカウントに紐づく定額制サービスの利用契約。利用許容量の推定対象となる。

**利用枠**  
契約内で設定される個別の利用制限単位（短時間枠、週次枠など）。

**対象期間**  
利用枠の消費量を同一基準で比較する期間。リセット予定時刻（`resetsAt`）を基準とし、日別・月別の集計期間とは区別する。

**利用実績**  
Hub から報告されるトークン消費量や API 換算額などの集計値。契約の実際の請求額とは異なる。

**API 換算額**  
消費したトークン量を従量課金 API 料金に換算した米ドル建て金額。

**消費率**  
利用枠に対する消費済みの割合（%）。

**観測**  
特定時点で取得された利用実績または利用枠の状態。

**増分**  
同一の契約・利用枠・対象期間における、2 点の観測間での API 換算額や消費率の増加量。

**利用許容量**  
利用枠を 100% 消費した場合に相当する米ドル換算の推定総量（実測に基づく参考値）。

**推定**  
同一対象期間における API 換算額と消費率の実測増分比から、利用許容量を算出すること。

**補完**  
未取得の過去利用実績を、Hub の保持履歴から取得して蓄積すること。

**リセット**  
利用枠の消費基準が更新され、以前の観測と同一期間として比較できなくなる事象。

**推定不可**  
観測データの不足や不整合により、利用許容量を算出できない状態。
