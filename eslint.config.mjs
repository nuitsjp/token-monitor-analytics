import js from '@eslint/js';
import tseslint from 'typescript-eslint';
import reactHooks from 'eslint-plugin-react-hooks';
import globals from 'globals';
export default tseslint.config({ ignores: ['dist/**', 'frontend/dist/**', 'frontend/src/routeTree.gen.ts', 'node_modules/**', '.e2e-results/**', 'playwright-report/**', 'release/**'] }, js.configs.recommended, ...tseslint.configs.recommended, { languageOptions: { globals: { ...globals.node, ...globals.browser, __MOCK__: 'readonly' } }, rules: { '@typescript-eslint/no-unused-vars': ['error', { argsIgnorePattern: '^_', varsIgnorePattern: '^_' }] } }, { files: ['frontend/src/**/*.{ts,tsx}'], plugins: { 'react-hooks': reactHooks }, rules: { ...reactHooks.configs.recommended.rules } }, { files: ['frontend/src/usecases/**/*.{ts,tsx}'], rules: { 'no-restricted-imports': ['error', { patterns: [{ group: ['**/backend/**', '**/features/client*', '@trpc/client'], message: '操作は機能アクセスを経由する。' }] }] } }, { files: ['frontend/src/features/**/*.{ts,tsx}'], rules: { 'no-restricted-imports': ['error', { patterns: [{ group: ['**/usecases/**', '**/routes/**'], message: '機能アクセスは画面に依存しない。' }] }] } }, { files: ['frontend/src/shared/**/*.{ts,tsx}'], rules: { 'no-restricted-imports': ['error', { patterns: [{ group: ['**/features/**', '**/usecases/**'], message: '共用UIに機能呼び出しを置かない。' }] }] } });
