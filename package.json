{
  "name": "token-monitor-analytics",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24.18.0 <25" },
  "scripts": {
    "start": "node --env-file=.env src/main.js",
    "mock": "node mock/hub.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check public/app.js && npm test"
  },
  "dependencies": { "eventsource-parser": "4.1.0" },
  "devDependencies": { "@playwright/cli": "0.1.11" }
}
