# 設計・開発計画と未決事項

本書は、開発の到達点、作業順序、未決・未検証事項、および確定済みの未実装機能に関する正本です。決定済みの動作は [機能仕様](doc/spec/functional-spec.md)、API 仕様は [連携仕様](doc/spec/interfaces.md)、アーキテクチャ構造・責務・保存境界は [設計書](docs/architecture.md)、製品要件は [設計方針](docs/design-policy.md) を参照してください。

## 1. 現在の到達点

- **U6修正・schema 10（2026-09-14、完了）**: レビュー9件に対応し、履歴APIの入力検証、schema 3〜9移行、再試行・再接続・停止再開の制御、停止保存失敗の表示、複合カーソル、履歴の閲覧・二期間比較を実装しました。端末日付・取得日時と前日実績の有無を保存して再起動後も表示できます。全202テストが成功し、失敗・スキップは0件です。実DB複製（38,461観測）・Mock複製（780観測）のschema 8→10移行で旧11表の件数、共通推定checkpointの一致、DB整合性・外部キー正常を確認しました。実HubのPrivate・Workから日次662行・月次128行を取得し、メモリDBへの保存・全ページ読み出しで重複なし、当日分除外を確認しました。Windowsの一時ヘッドレスEdgeで、221件・3ページの絞り込み・更新・二期間比較・追加表示、欠測・途中集計・取得状態、読み出し失敗時の表示保持を確認しました。390px・1440pxともページ全体の横はみ出しはありません。証跡は `.local/u6-completion/` の `tests.txt`、`migration.json`、`real-hub-history.json`、`ui-result.txt`、`ui-status-result.txt`、`ui-390.png`、`ui-1440.png` です。

- **初回U6・schema 9の記録（2026-09-14、完了判定を訂正）**: 初回174テストは成功しましたが、レビューで入力不正による全Hub保存停止、移行・通信・停止再開・ページングの問題と表示未実装が見つかりました。当時の実装・検証完了という判定を撤回し、上記schema 10の修正・検証結果へ置き換えました。

- **Astraレビュー・schema 8（2026-09-14、修正・検証完了）**: schema 7の再生が当時未保存の欠測・鮮度・通知境界を正常と仮定していた問題、移行途中の失敗が全段階を戻さない問題、異常終了後の中断状態が消える問題を修正しました。schema 8では受信日時、通知単位の観測ID参照、端末の欠測・鮮度、Hubの中断状態、当時の契約対応と推定設定を `estimation_inputs` に順序付きで保存します。観測・現在値・推定checkpoint・イベントと同一トランザクションで確定し、再計算はこの入力履歴を逐次再生してcheckpointだけを更新します。追記済みイベントは変更しません。schema 7以前には通知境界や欠測情報の履歴がないため、観測行だけから正常な入力を仮定して再生しません。既存checkpointを再生の開始点として保存し、schema 7で再生成された比較基準だけは今回一度無効化します。既存の結果・観測・イベントは保持し、以後の正常終了を伴う再起動では比較基準を継続します。 自動テスト159件が成功し、失敗・スキップは0件です。Real 37,464観測・Mock 780観測を保持してschema 8へ移行し、元8表の全行SHA-256一致、整合性・外部キー検査正常、既存結果の保持（Real 5件・Mock 4件）を確認しました。修正後の実受信入力の再生はcheckpointと完全一致し、正常再起動ではcheckpoint・イベントが不変でした。05:56 JSTにPrivate・Workの接続・保存正常と受信日時の前進を確認しました。証跡は `.local/astra-review/manifest.json`、`restart-checkpoint.json`、`runtime-after-restart.json`、テスト出力は `.local/astra-tests.txt` です。照合スクリプトでは相対パス誤りとSQLite返却オブジェクトの型比較で一度ずつ失敗し、修正後に照合を完了しました。UI変更はなく、今回はブラウザの再検証は行わず、自動テストと実APIで確認しました。
- **過去のschema 7検証（2026-09-13）**: 150件のテスト、Real 19,452観測・Mock 780観測の8表ハッシュ一致、結果とイベントの保持、再起動後の受信継続を確認しました。ただし上記の入力欠落を検証していなかったため、推定継続修正の完了判定を撤回し、schema 8で修正しました。証跡は `.local/estimation-continuity/20260913T120942113Z/manifest.json` です。
- **Cursorの重複除去・共通の端末除外（2026-09-13）**: 利用者の指示に基づき、Cursorのアカウント全体実績を同じ契約集合につき一度だけ数え、Hub内・全体の利用額とトークン数も重複除去します。推定では全サービス共通で、古い報告・費用欠落・収集失敗などの端末を除外し、採用できる端末の実測増分から参考値を算出します。対象が変われば比較区間を切り替え、除外した端末と理由を表示します。以前の「既知端末が欠けたら全体を停止」という条件をこの指示で変更し、通常の設定不要・異なるプランの共有利用額だけ倍率設定という合意は維持します。推定の端末除外と、保存済み利用実績の集計は区別し、後者では端末が古いだけで過去の利用額を消しません。148件の自動テストが成功し、失敗・スキップはありません。実DB16,579観測・Mock780観測をschema 5から6へ移行し、観測・現在値・旧/共通推定events・契約関連を含む8表の全行SHA256一致とDB整合性を確認しました。実DBの旧30系列・2結果、Mockの旧2系列・2結果を保持し、新しい比較点は過去データから生成していません。19:57:00 JSTに最終コードを `mise run real` で再起動し、19:58も両Hubの受信・保存継続を確認しました。headless EdgeでPrivate Cursorは1報告を採用・1報告を重複として不採用、GrokはPrivateの2端末を採用・WorkのZRFG065603を古い報告、ZRFG067961を費用欠落として除外し、旧Cursor 749.816 USDの履歴分離・履歴取得、GrokとPro5xの各1契約表示を確認しました。390px・1440pxで横はみ出し0、JavaScriptエラー0、全3期間のメトリクス表示も確認しました。20:05 JSTにも両Hubの保存が正常で受信日時が進んでいることを確認し、新方式のCursor・Grokは比較起点が作られた後の増分待ち（no_increase）です。旧結果は流用していません。証跡は `.local/usage-scope/2026-09-13T10-52-17-756Z/manifest.json`、同ディレクトリの退避DB、`.local/usage-scope/final-tests.txt`・`runtime-verification.json`・`ui-verification.txt`・`dashboard.png` です。以下のschema 3〜5の記録は当時の検証事実です。

- **過去の全サービス共通推定・schema 5の実装と検証（2026-09-13）**: 利用者の「全サービスを個別契約・個別実装せず同一設計で対応する」という指示に基づき、サービス名と通常枠に限定する条件を廃止しました。未知のproviderを含む共通入力、割合・残量率・使用量と上限、任意の枠種別・追加枠、ラベルによる枠識別、情報不足、同時刻の競合、上限変更、DB移行と失敗時の復元を含む全137テストが成功し、失敗・スキップはありません。単一契約・同一プランでは倍率設定不要という合意、Grokのメール識別、Hubを変更しない制約を維持しています。実DBは観測13,456件、Mock DBは780件を保持してschema 4→5へ移行しました。観測・現在値・旧/共通推定eventsを順序付きSHA256で照合し、既存Codex/Grokの契約関連とcheckpointも不変です。実DBの契約9件を8件に統合し、関係21件を保持、両DBのintegrity/foreign key検査は正常でした。18:34:25 JSTに最終コードを `mise run real` で起動し、headless EdgeでPrivate 2端末・Work 5端末、Cursor billing枠の共通推定開始、サービスによる対象外0件、既存Codex推定保持、Pro5x 1契約3端末、Grok 1契約4端末、390px/1440pxで横はみ出し0・JavaScriptエラー0を確認しました。18:36 JSTには両Hubの受信・保存継続も確認しました。Cursorは増分待ち、Grokは古い端末データにより待機中です。全サービスの実契約検証は実施せず、共通入力テストで処理を検証しています。証跡は `.local/generic-estimation/2026-09-13T09-30-12-675Z/manifest.json`、`.local/generic-estimation/final-tests.txt`、`.local/generic-estimation/runtime-verification.json`、`.local/generic-estimation/dashboard.png`、`.local/verify-generic-ui.js` です。以下のschema 3/4記録は当時の検証事実であり、現在の対応制限ではありません。

- **現在値機能を実装済み**: 起動・設定、現在値の受信・保存・表示、自動再接続を実装し、Mock Hub による自動テストと Windows の Chrome で検証しました。実稼働中の Private・Work の両 Hub への同時接続・保存と、利用者による画面表示も確認済みです。[機能仕様](doc/spec/functional-spec.md#scenarios)・[設計書](docs/architecture.md) に初期版全体の動作と責務を定義していますが、[未決事項](#open-items)に依存するシナリオが残るため、初期設計・製品初期版は未完了です。
- **schema 3時点の共通契約実装・検証記録**: Codexの通常枠について、Hub横断の共通契約・端末と契約のn:n関連・schema 3・全体API/UIを実装しました。自動テスト115件、実DBとMock本体への移行・旧5表の保全照合、Mockからrealへの切替、headless Edgeでの共通契約表示・履歴・メーター・全体メトリクスを検証済みです。旧方式の推定結果は履歴として保持しています。日次・月次実績の履歴は順序3の残件です。
- **Grok契約識別・schema 4の検証記録（2026-09-13）**: `provider=grok` と正規化した `accountEmail` による契約対応、トークン変更の同一契約化、メール別契約分離、欠落メールの端末情報保持を実装・検証しました。最終コードでは全122件のテストを実行し、失敗・スキップはありませんでした。Grokの比較ラベルだけの差を競合扱いしない回帰と、最新時刻の `notConfigured`・`disabled` によって古い失敗契約を復活させない回帰を含みます。実DBのschema 3→4移行ではGrok契約19件を1件（関係4端末、現在1契約）へ再構成し、観測12,091行、旧方式events705行、共通events412行、Grok以外の契約8件・関連17件、他providerのcheckpointを移行前後で保全しました。`integrity_check`・`foreign_key_check` は正常でした。Mock DBでも観測780行、旧方式events66行、共通events24行を不変で確認しました。17:53:11 JSTの `mise run real` 再起動後、headless EdgeでGrokカード1件・関係4端末・メーター1件・Hub2＋端末4の参照6件、Codex Pro5xの1契約3端末、両Hubの受信日時前進、390px・1440pxの横はみ出し0、JavaScriptエラー0を確認しました。証跡は `.local/grok-migration/2026-09-13T08-46-49-120Z/manifest.json`、`.local/grok-contract-after.png`、`.local/verify-grok-ui.js` です。コードレビューで確認した表示ラベルによる誤競合を修正し、回帰試験と最終コードの実画面確認まで完了しました。
- **仕様の整理**: 機能仕様および設計方針を正本として仕様を整理済みです。具体的な未決・未検証事項は U1〜U11 として管理しています。
- **汎用テンプレートの分離**: 設計・文書管理およびモック駆動開発の共通枠組みは、独立した配布物 [nuitsjp/aidd-project-template](https://github.com/nuitsjp/aidd-project-template) へ分離しました。モック駆動開発の既定採用（U10）とツール採否（U11）は未決です。
- **実装の出発点**: 旧実装・テスト・ローカル DB・設定を再利用せず、現行仕様に基づいて基礎環境を構築しました。残りの未実装機能は [第5節](#unimplemented) で追跡します。
- **調査資料の管理**: Hub API の調査事実は [連携仕様](doc/spec/interfaces.md)、Private Hub の実応答サンプルは [docs/reference/hub-private](docs/reference/hub-private/README.md) に記録済みです（単発調査であり、現行実装の動作検証ではありません）。
- **利用者認証の後続化**: 初期版は限定された安全な利用環境を想定し、ダッシュボードの利用者認証は後続実装とします（[U12](#u12)）。外部 Hub 接続用の共有シークレット認証、入力検証、CSRF 対策は初期版の対象です。
- **旧方式の検証記録**: 2026-09-13、Windows・Node.js v24.18.0 で自動テスト103件が成功し、失敗・スキップはありません。これは従来のHub単位方式を対象とした結果です。従来方式については推定の式・共有範囲・部分更新・リセット・欠測・再受信・再起動・保存失敗・移行・外部読み取りロック・モード分離・二重起動・強制終了後の起動を含みます。ヘッドレス Edge で従来方式の推定値、根拠、履歴ページング、設定不足の説明、390px・1440px幅を確認しました。実データ用・Mock用の mise タスクの起動・切り替えと、保存値・履歴の分離も確認しました。別端末からの現在値の LAN 表示・継続更新は利用者が確認済みです。実機検証の残件は [U1](#u1)、共通契約方式の残件は [U2〜U5](#u2)、確認根拠は [設計書 第9節](docs/architecture.md#quality-and-risks) に記録します。
- **schema 3時点の共通契約変更の検証記録**: 移行検証用スクリプトを作成し、`mise tasks validate` の2件と `npm test` の115件が成功しました。実DBコピーのschema 3移行は7.492秒で完了し、旧5表の順序付きSHA256照合で `hubs` 2行、`observations` 9,183行、`current_devices` 7行、`estimation_hubs` 2行、`estimation_events` 705行の不変を確認しました。`contracts` 25件、`device_contracts` 34件を生成し、現在のPro5xは1契約・Private `home-main` とWork `zrfg050641`・`zrfg066335` の3ソースになりました。移行直後の共通イベントは0件、baseline/latestはすべてnullで、旧比較点を流用していません。SQLiteの `integrity_check` は `ok`、`foreign_key_check` は空でした。Mockコピーでも旧観測758件・旧イベント66件、契約1件・関連2件の保全を確認しました。実DBとMock本体にもschema 3を適用し、同じ旧5表の全行不変・整合性正常・新比較点未流用を確認しました。`mise run mock` から停止して `mise run real` へ切り替え、headless EdgeでMockの契約カード1枚、realの契約カード25枚、real Pro5xの1契約3端末、Hub内メーター重複0、全体USD合計、Hub日時、Weekly・残量、`notConfigured` の既定非表示とtoggle表示、共通・旧履歴のscope分離・旧Hub絞り込み・ページング、390px・1440pxの横はみ出しなし、両Hubの更新継続、JavaScriptエラー0件を確認しました。画面の軽微な幅と空ラベル表示を修正し、最終headless再確認も完了しました。証跡は `.local/shared-contract-migration/20260913-153830/manifest.json`、`.local/verify-shared-ui.js`、`.local/shared-contract-migration/20260913-153830/real-ui-verification.json`、`.local/shared-contracts-pro5x.png` にあります。共通契約方式の今回採用範囲は検証済みです。初期版全体の日次・月次履歴、Hub管理、手動更新は未完了です。
- [x] **旧資産の削除**: 不要となった旧実装・旧設定（`old/`）の削除を完了しました。

**schema 3移行時の稼働記録:** 当時のコードを15:45:42 JSTに `mise run real` で起動し、Private・Workの受信・保存を確認しました。15:48 JSTのAPI確認では、Pro 5xの通常Weekly枠は全3ソースの新しい観測待ちで、新方式の実契約の推定値はまだ生成されていません。150 USDの検証値は制御した自動テストの結果です。

### 全体の進捗と次の作業（2026-09-14）

| 順序 | 現在の状態 |
| :---: | --- |
| 1 | 現在値の受信・保存・表示、再接続、Real／Mockの排他起動は実装・検証済み。実機確認の残件はU1に保持 |
| 2 | 全サービス共通推定、契約の重複除去、推定履歴は実装済み。今回のAstraレビューで更新・異常終了後の継続を修正。検証結果は第1節とU3に記載 |
| 3 | 日次・月次利用実績の収集・保存・参照とHub個別の停止・再開は実装済み。Hub管理画面と管理操作のCSRF対策は未実装 |
| 4 | 手動アップデート・失敗時の復旧支援は未実装。長時間稼働や実障害などU1・U9の実機確認も残る |

次は **U7のHub管理画面とCSRF対策** を進めます。U6で実装した停止・再開の内部操作を画面から変更できるようにし、管理APIの入力検証とCSRF対策を具体化します。U8の手動更新・復旧支援、U10・U11の開発補助方針、U12の利用者認証は未決／後続のままです。

## 2. 作業の進め方と順序

| 順序 | 作業内容 | 完了条件 |
| :---: | --- | --- |
| 1 | 起動・設定・Mock Hub・検証環境を構築し、現在値の受信・検証・保存・表示・再接続を実装する。必要な U6・U7・U9 の境界を先行して具体化する | Windows 環境で現在値を保存・表示し、重複受信、不正入力、保存失敗、再起動を検証できること。推定機能は未実装と明示する |
| 2 | U2・U4 の契約帰属・利用枠識別と、U3・U5 の比較基準・結果保存を確定し、Hub横断の共通契約推定・Grok契約表示・履歴表示を実装する | 全providerの provider・accountKeyで共通契約を一件として扱い、Grokは正規化した accountEmailで一件として扱う。サービス名で制限せず、不十分な端末を除いた採用ソースの正常で新しい観測がそろった時だけ共通推定を確定し、リセット・欠測・再受信・再起動時にも旧履歴・新しい根拠と結果を保全できること。Grokのschema 4移行では観測・推定events・他provider契約関連を変更しないこと |
| 3 | U6・U7・U9 の履歴取得・並行制御・Hub 管理・収集停止を具体化し、実装する | 日付境界、通信・保存失敗、複数 Hub、停止・再開、設定復元を検証できること |
| 4 | U8 の手動更新・復旧を実装し、運用手順の策定と U1 の実機検証残件を完了する | 初期版の受け入れ条件を満たし、Windows および実 Hub で必要な検証を終え、必須修正が残っていないこと |

※ 順序1と並行して U2・U4 の調査を進めることが可能です。また順序2と3は独立して進められます。各機能の実装に伴って U1 の実機確認を行い、順序4で残る範囲を検証します。仕様の確定事項は `doc/spec/`、アーキテクチャ上の責務は設計書へ反映します。モック駆動開発の採否（U10）および補助ツール選定（U11）は個別に判断します。

### 完了済み：現在値の受信・保存・表示

**ヒアリング状況:** 2026-09-13 に、通知内の不正データの扱い、保存障害、通知不達、起動時と稼働中の DB 障害、一部 Hub の設定不正について6件の運用方針を合意し、[機能仕様 S1](doc/spec/functional-spec.md#s1)・[S6](doc/spec/functional-spec.md#s6)・[S7](doc/spec/functional-spec.md#s7) に動作と検証条件を反映しました。現在値機能の実装と自動検証は完了しています。接続設定の残件は [U7](#u7)、保存障害と通知不達の残件は [U9](#u9) で追跡します。

**追加ヒアリング（2026-09-13）:** 1-1でデータ側と Analytics 側の日時を分ける方針、1-2で保存失敗時は数値と両方の日時を維持し、受信日時の表示名を「最終保存成功データの受信日時」とする方針を合意しました。[機能仕様 第2節](doc/spec/functional-spec.md#display)・[S1](doc/spec/functional-spec.md#s1)・[S6](doc/spec/functional-spec.md#s6) と [設計書 S1](docs/architecture.md#s1)・[S6](docs/architecture.md#s6) に反映済みです。表示日時と Hub 項目の対応も機能仕様 第2節へ具体化しました。現在値の実装・自動検証は完了しています。実機検証の残件は U1 で追跡します。

追加ヒアリング1-3では、現在値機能の先行実装は設定ファイルによる Hub 接続設定から開始し、Web 画面からの登録・変更は順序3で実装する方針を合意しました。動作と検証条件は [機能仕様 S7](doc/spec/functional-spec.md#s7)、読み込み担当は [設計書 第7節](docs/architecture.md#7-配置と運用-deployment-view) に反映済みです。設定の詳細は U7 で追跡し、現在値の実装・自動検証は完了しています。実機検証の残件は U1 で追跡します。

追加ヒアリング1-4では、先行実装の共有シークレットを Git 管理対象外のローカル設定ファイルへ平文保存し、Windows のファイル権限で保護する方針を合意しました。[機能仕様 S7](doc/spec/functional-spec.md#s7) と [設計書 第7節](docs/architecture.md#7-配置と運用-deployment-view) に反映済みです。設定例・権限設定スクリプトを実装し、Windows PowerShell 5.1 と実フォルダーで継承除外および実行ユーザー・SYSTEM・Administrators の3者への許可を確認しました。

追加ヒアリング1-5では、設定ファイル全体を読み込めない場合は起動を中止し、設定修正後に手動で再起動する方針を合意しました。[機能仕様 S7](doc/spec/functional-spec.md#s7) に動作・検証条件、[設計書 S7](docs/architecture.md#s7) に処理境界を反映済みです。現在値の実装・自動検証は完了しています。実機検証の残件は U1 で追跡します。

追加ヒアリング1-6では、数値や取得状態が同じでもデータ側の更新日時が進んだ場合は観測を追記し、集計・配信・Analytics 受信の時刻だけが変わる再配信は重複保存しない方針を合意しました。[機能仕様 S1](doc/spec/functional-spec.md#s1) に動作・検証条件、[設計書 第5.3節](docs/architecture.md#53-保存モデルと確定点) に比較項目・正規化・保存単位を反映済みです。固定版ソースでの日時項目の確認結果は [連携仕様 第3.1節](doc/spec/interfaces.md#31-sse-の通知) に追記しました。現在値の実装・自動検証は完了しています。実機検証の残件は U1 で追跡します。

**現在値機能の設計具体化:** 設定ファイルの形式・項目と Hub 登録情報との対応、ブラウザ再接続時の状態配信、保存停止中の受信、起動終了の順序を機能仕様・設計書へ反映しました。設定例・権限設定コマンドを含む基礎環境の構築と、設計に基づく現在値の実装・自動検証は完了しています。実機検証の残件は U1 で追跡します。

追加ヒアリング1-7では、同じネットワークの別端末からも Web 画面を利用する方針を合意しました。待受アドレス・ポートと変更反映の契約を [機能仕様 S7](doc/spec/functional-spec.md#s7)、起動責務と配置を [設計書 S7](docs/architecture.md#s7)・[第7節](docs/architecture.md#7-配置と運用-deployment-view) に反映しました。

ヒアリング1-8では、2026-09-13 に同じネットワークの別端末から `http://192.168.0.17:3000/` を開き、Mock データの画面表示と約5秒ごとの金額更新が動作することを利用者が確認しました。別端末からの JSON API の直接取得、通信切断後の再接続、長時間稼働は確認対象に含めておらず、U1 の残件として維持します。

ヒアリング1-9・1-10では、Private Hub に加えて Work Hub も接続対象とすることを利用者が指定し、両方を `.local/hubs.json` に設定しました。現在値経路で Private の2端末、Work の5端末を同時に受信・保存し、両 Hub の受信日時の前進、SQLite への確定、ログ・JSON API にシークレットが含まれないことを確認しました。1-11では、利用者が Mock の保存値と Private・Work の実データを画面で確認しました。当時は Mock の接続設定を外し、保存済みデータを「接続設定なし」として表示していました。その後の利用者指示により、現在は [実データと Mock の分離](#mode-separation) を適用しています。

検証は API・SQLite・自動テストなどヘッドレスで完結できる方法を優先します。ブラウザの自動操作が必要な場合も、ログイン済みセッションを必要としない画面は、利用者の指定に従いヘッドレスで確認します。

- [x] S1・S3・S6・S7 に必要な現在値の受信・重複判定・一括保存・状態通知・起動終了の境界と検証条件を具体化する（推定・履歴・管理操作は後段）。
- [x] Node.js 24、SQLite、起動・終了コマンド、設定例、Mock Hub、テスト実行環境を構築する。
- [x] SSE 接続・入力検証・観測保存・現在値表示・再接続を実装する（Hub と端末の分離、各種状態の区別を含む）。
- [x] Mock Hub を用いて通常受信、重複受信、不正入力、切断、保存失敗、再起動、秘密情報の非露出を自動検証する。
- [x] Windows の Chrome で受信・表示・継続更新・ブラウザ自動再接続を確認する。
- [x] 同じネットワークの別端末から、Mock データの画面表示と SSE による継続更新を確認する（ヒアリング1-8、利用者確認）。
- [x] 実 Hub で受信・保存・表示を確認し、継続接続等の未検証範囲を U1 に記録する（Private 2端末・Work 5端末、画面は利用者とヘッドレス Edge で確認）。

### 順序2の実装・検証結果（2026-09-13）

利用者の「では自律的に遂行してください」の指示を受け、以下を実施しました。採用範囲と停止理由は機能仕様第1.5〜1.6節、責務と保存境界は設計書第5節に反映しています。

- [x] ツール別累積額、観測上のアカウント識別、同じ種類の枠、利用実績と利用枠それぞれの取得日時を用いて比較範囲を定義する。
- [x] 通常時は設定不要とし、異なるプランの共有利用額だけに基準プラン・倍率を適用する。
- [x] 推定・観測・比較基準・算出根拠・状態変更を通知単位で一括保存し、保存済み結果の履歴照会を実装する。
- [x] リセット・欠測・古い通知・再起動・契約や端末の変化で不明区間をまたがず、累積額だけの減少では起点を維持することを検証する。
- [x] 推定結果を保存できない場合の全体ロールバック、保存停止後の履歴閲覧、参照不能時の503応答を検証する。
- [x] ヘッドレス Edge で単一100 USD・同プラン共有100/100 USD・倍率1/5共有100/500 USD、履歴20件から26件への追加読み込み、設定不足の説明、390px幅を確認する。
- [x] 旧方式の実DB複製でschema1→2を移行し、3 Hub・9端末の現在値・3,324観測の全行ハッシュ一致と、過去の推定が生成されないことを確認する。これは今回のschema 3・共通契約方式の移行検証ではない。
- [x] 12:04 JSTに実アプリを更新して両Hubの接続・保存を再開する。実DBでも旧3,324観測のハッシュ一致を確認し、12:05 JSTにPrivateの実観測から推定結果が生成されたことと、API・履歴・ログの秘密非露出を確認する。

検証用のDB・Mockと実運用のDBは分けて実行しました。再起動・切断等の境界条件は制御できるMockで検証しており、実Hubの全アカウントでリセットや切替を実演したものではありません。Workでは古い端末報告を含む対象の推定が停止し、別の対象の観測は継続しています。

**検証中の保存停止と復旧:** 12:06 JST、実DBの旧観測のハッシュ照合を稼働中に行った際に `save-notification / ERR_SQLITE_ERROR` が発生し、全Hubの保存が停止しました。独立した読み取り専用接続がロックを保持すると `errcode=5 / database is locked` でCOMMITが失敗することを一時DBで再現しました。実例は当時数値コード未記録のため、同時刻に実行した外部読み取りとの競合が原因と推定します。失敗時の旧値・観測数保持と、読者解放後の保存成功を自動テストへ追加しました。ログにSQLite数値コードを追加し、全件照合を停止中の複製で行う運用を設計書へ記録しました。外部読者の終了後、12:13 JSTに再起動して保存を復旧し、12:14 JSTに両Hubの継続更新、Privateの保持済み推定・履歴、390px・1440px幅、JavaScriptエラーなしを確認しました。最初のブラウザ継続更新テストのタイムアウトはこの保存停止によるもので、復旧後の再検証は成功しました。

<a id="mode-separation"></a>
### 追加依頼：実データと Mock の排他起動（2026-09-13）

利用者の指定に従い、`mise run real` と `mise run mock` を実装しました。動作は [機能仕様 S7](doc/spec/functional-spec.md#s7)、起動責務・保存先・運用手順は [設計書 S7](docs/architecture.md#s7)・[第7節](docs/architecture.md#7-配置と運用-deployment-view) に反映しています。

- [x] 共通の待受設定を維持し、Hub接続設定・DB・ログを分離する。Mock の起動では実 Hub 設定ファイルを読まず、内蔵 Mock Hub と Analytics を同じプロセスで起動・終了する。
- [x] 作業ディレクトリ単位の名前付きパイプで二重起動を拒否する。ポートが異なる場合、同モードの場合、通常終了・起動失敗・強制終了後の起動を一時環境で自動検証する。
- [x] 旧アプリ停止中に全5表を分離する。実データ側は2 Hub・7端末・5,365観測・推定checkpoint2件・推定イベント292件、Mock側は1 Hub・2端末・694観測で、各表の全行ハッシュと件数が元の対象行と一致した。両DBの整合性・外部キー検査も成功し、旧 `data/analytics.sqlite` 自体のハッシュは変更前後で一致した。照合結果は Git 管理対象外の `.local/mode-split-verification.json` に記録した。
- [x] mise 2026.8.6 で両タスクを実際に起動する。Mock 稼働中の real 起動と、real 稼働中の Mock 起動は `RUNTIME_IN_USE`・終了コード1で拒否された。Mock の Ctrl+C 後に real へ切り替わり、非稼働側DBのファイルハッシュも一致した。
- [x] ヘッドレス Edge で Mock の2端末と、実データの Private 2端末・Work 5端末を別々に確認する。他モードの現在値・履歴は表示されず、390px・1440px幅でモード表示、継続更新、ページのはみ出しなし、JavaScriptエラーなしを確認した。実データの既存推定イベント292件は起動後の履歴APIでも内容が一致し、API・両ログに実 Hub のシークレットが含まれないことを確認した。
- [x] 取得状態が `notConfigured` のプロバイダーを既定で隠し、画面上部のトグルで表示できるようにする。空の Codex weekly ラベルを `Weekly` と表示し、利用枠の表示率とメーターを残量（未使用100%から減少）へ統一する。実データ画面で既定時の非表示、トグル切替、Weekly ラベル、残量表示をヘッドレスで確認した。
- [x] 画面最上段に各Hubの集計から期間別に合算した全体メトリクスを表示し、各Hubに「Hub 最終更新日時（データ側）」と「最終保存成功データの受信日時」を表示する。欠落値をゼロ補完しないことを含め、実データ画面で確認した。

検証後は `mise run real` で実データ収集を再開しました。実データ画面は既存の `http://192.168.0.17:3000/` です。順序3以降と U1 の長時間稼働などの未完了項目は従来どおり維持します。

## 3. 完了の判定

- **初期設計の完了**: 初期版の S1〜S9 について [文書方針の5項目](docs/document-policy.md#initial-design-completion) を具体的に説明できた時点。
- **製品初期版の完了**: 実装および検証が [設計方針の受け入れ条件](docs/design-policy.md#product-completion) を満たした時点（利用者認証、Linux 対応、端末の手動関連付けは後続スコープ）。U12 の未決・未実装は初期版の完了を妨げません。

<a id="open-items"></a>
## 4. 未決事項・未検証事項

未決・未検証の課題は以下の ID で管理し、解消条件が満たされるまで追跡します。

| ID | 状態 | 依存・影響範囲 |
| --- | :---: | --- |
| [U1](#u1) 実 Hub と対応環境 | 未検証 | API 互換性、継続受信・再接続、Windows 実機検証（Linux は後続） |
| [U2](#u2) 利用額の契約帰属 | 調査・設計・コード・自動テスト・実DB・headless検証済み | 全providerの共通推定を実装。provider+accountKey（Grokは正規化メール）でHub横断の契約を対応付け、情報不足時は理由表示 |
| [U3](#u3) 比較基準と推定の確定 | 設計・コード・自動テスト・実DB・headless検証済み | 全関係ソースの新しい観測を待つ共通状態、共通・旧方式履歴の分離、境界後の再基準化 |
| [U4](#u4) 利用枠の同一性 | 調査・設計・コード・自動テスト・実DB・headless検証済み | 全providerのHub横断キーと枠構造キーで照合。limitIdがない枠は前後空白を除いたlabelも使う |
| [U5](#u5) 端末別の比較基準 | 設計・コード・自動テスト・実DB・headless検証済み | Hub・端末分離、端末・契約n:n関連、共通集合、構成変化と不足ソースの扱い |
| [U6](#u6) 履歴・並行制御・収集停止 | 完了 | S2・S5・S7。自動・実Hub取得・DB複製移行・Windows画面の検証済み |
| [U7](#u7) Hub 管理と秘密情報 | 未決 | U6 の停止・再開に依存。S5・S8（Hub 接続設定・秘密情報保護・CSRF 対策） |
| [U8](#u8) 手動更新と復旧 | 未決 | S9（互換性確認・履歴保全）。データ消失リスク等は事前合意を要する |
| [U9](#u9) 保存障害の確定点と影響範囲 | 未決・要検証 | S6・S7 および管理操作。共有 DB 障害時の他 Hub への影響分離 |
| [U10](#u10) モック駆動開発の既定採用 | 未決 | UI 変更フローと合意記録。内部ロジックや既存仕様修正とは独立 |
| [U11](#u11) モック開発の補助製品 | 未決・要確認 | 対象 UI・通信境界と開発構成。プロセスの採否とは別に判断 |
| [U12](#u12) 利用者認証 | 後続・未決 | 初期版完了後の認証方式・適用時期・Web のアクセス制御。初期版の完了判定から除外 |

<a id="u1"></a>
### U1. 実 Hub と対応環境の確認

**現状:** 上流コミット `2f60827e` の調査および [Private Hub の実データ資料](docs/reference/hub-private/README.md) に基づいて実装しました。2026-09-13 に Windows 上の Mock Hub と Chrome（playwright-cli 0.1.11、ユーザー指定）で表示・継続更新・アプリ再起動後の再接続を確認しました。同日のヒアリング1-8で、別端末からの LAN 経由の画面表示・継続更新も利用者が確認しました。その後、Private・Work の実 Hub へ同時接続し、初回と後続通知の受理・保存、利用者による画面表示を確認しました。接続先とビルド情報は [連携仕様 第1節](doc/spec/interfaces.md#1-調査根拠)、実行条件と検証範囲は [設計書 第9節](docs/architecture.md#quality-and-risks) に記録しています。

調査した API の項目・制約・確認範囲は [連携仕様](doc/spec/interfaces.md) を参照します。

- [x] 実 Hub に対する初回受信と短時間の後続受信・保存・表示を検証する（Private・Work）。
- [ ] 実 Hub の通信切断・自動再接続と長時間稼働を実機検証する。
- [ ] 調査基準コミットと文書の比較対象版（`c4182fd5`）の差分を精査し、API・識別項目・履歴・利用枠の互換性を確認する。
- [ ] 各機能の実装後、Windows 実機で受け入れ条件を検証する（Linux 対応は後続）。
- [x] 同じネットワークの別端末から、LAN 側の待受先に対する画面表示と SSE による継続更新を検証する（Mock データ、利用者確認）。
- [ ] 同じネットワークの別端末から、JSON API の直接取得と通信切断後の SSE 再接続を検証する。

**解消条件:** 実機環境での検証シナリオと結果が記録され、未検証範囲が明確になっていること（Mock Hub のみで代替しない）。

<a id="u2"></a>
### U2. 利用額を契約・利用枠へ帰属させる根拠

**現状:** Hub からの実績データは端末・ツール別の累積 API 換算額を含みますが、実績明細へ契約 ID は付与されません。個々の契約への金額分離を一律に求めず、利用額に含まれる契約集合と全契約の消費率が対応することを確認します。名称一致だけで機械的に結合せず、この対応が不明な場合は「推定不可」とします（取得値は集計単位を明記して表示）。観測・端末はHubと `deviceId` の組で保持し、全providerで `provider + accountKey` をHub横断の観測上の同一契約として扱います。Grokは正規化した `accountEmail` でHubをまたいで同一契約として扱い、トークンの `accountKey` 変更では分割しません。全サービスに同じ計算処理を適用し、取得情報が不足する場合は具体的な理由を表示します。

**利用状況の確認（2026-09-13）:** ヒアリング2-1で、Private・Work の端末におけるアカウント・契約の共用について、利用者は「すべて別のアカウント・契約を使っている」と回答しました。2-2では、同じ端末・同じツールでアカウント・契約を切り替えることがあると確認しました。2-3では利用者が Private Hub の Codex を実例として示しました。ツール一覧の申告要求は撤回し、取得データから判定できることを先に調査しました。

**Hub横断の調査結果（2026-09-13実GET）:** Private の `home-main` と Work の `zrfg050641`・`zrfg066335` のCodex Pro 5x行で `accountKey` が一致しました。全7端末のHub間 `deviceId` は重複せず、当日・当月の返却セッションにもHub間の重複はありませんでした。`allTime` に利用明細がないため、この確認だけで全期間の利用額が契約へ完全に帰属することは証明できません。上流のキー生成がHub非依存である根拠と確認範囲は [連携仕様 第3.5節](doc/spec/interfaces.md#35-hub横断同一性の確認範囲-2026-09-13) に記録しました。

**切替の調査結果:** 固定版の上流コードでは、複数アカウントの実績がツール別の合計に混在し、過去ログの遅延集計もあり得ます。`accountKey` の変化をまたぐ区間を除くだけでは、その後の増分が新しい契約だけの利用分であることを証明できません。根拠は [連携仕様 第3.3節](doc/spec/interfaces.md#33-アカウント切替時の契約帰属の制約) に記録しました。これは個別金額の帰属の限界であり、共有利用額からの推定が一律に不可能という意味ではありません。

**推定方式の訂正（2026-09-13 利用者指示）:** 利用者は、以前の基準プラン（x1）とプラン別倍率を用いる理論を示し、それ以外では基本的に基準・倍率の設定なしで動作することを求めました。旧コミット `81672a8` の `docs/requirements.md` にも、DM-PLAN-08・P1-EST-06〜08・AC-P1-18 として、同一プランの契約には共通許容量を使い、異なるプランの共有利用額には基準プランと倍率を使う方式を確認しました。この理論を現行の [機能仕様 第1.2節](doc/spec/functional-spec.md#12-計算式と成立条件) へ反映しました。契約別の金額分離を一律に要求していた説明を訂正し、U2 の残件を合計利用額と対象契約集合の対応へ整理します。旧版のカタログ管理全体、隣接観測のみの計算、最小二乗法、履歴の再計算等は今回の変更に含めません。

通常時は基準プラン・倍率の入力を要求せず、同一プランの複数契約も設定不要とします。異なるプランでも利用額を個別に分離できれば倍率不要です。異なるプランの共有利用額をまとめる場合だけ、Hub共通のトップレベル設定を求めます。旧方式の推定およびその設定による式の三例は実装・検証済みですが、Hub横断の共通契約方式には流用しません。

**設定位置の確定（2026-09-13 利用者指示）:** `hubs[].estimation` は後方互換として読み替えず、存在時は `ConfigurationError('hub_estimation_must_be_top_level')` を記録してHTTP待受・Hub接続前に起動を中止します。トップレベル `estimation` の形式不正は全体の推定だけを停止し、Hubの接続・観測保存は継続します。実際の設定ファイルには旧Hub行設定は存在しません。コードと自動テストで確認済みです。

過去の会話も読み取り専用で確認し、2026-09-12 10:45:43 JST の利用者発言「複数プランがあった場合、ベースプランに対する倍率指定することと、利用率と全体の利用額から比例計算で求める形」を確認しました。今回の仕様訂正は、この理論と通常時の設定不要という最新指示に基づきます。

取得項目は [連携仕様](doc/spec/interfaces.md#observation-fields)、成立条件は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [x] 採用する実績項目と集計期間、利用額ソースと対象契約集合の対応規則、必要情報がない場合の停止範囲を調査・設計する。共有利用額の契約別分離は必須としない。
- [x] 全providerの `provider + accountKey`（Grokは正規化メール）をHub横断の観測上の同一性に採用する。2026-09-13の利用者指示によりCodex限定を廃止する。
- [x] 同一プランの識別、同一契約の複数Hub・端末報告の重複除去、契約切替、モデル限定枠などの判定規則を設計する。
- [x] 異なるプランの共有利用額に必要な基準プラン・倍率の設定をトップレベルへ置く方針を確定する（通常時は入力不要。計算に使用した設定値は根拠に保存）。
- [x] schema 3 の `contracts` と `device_contracts` へ全観測・旧registryの関係を移行するコードを実装し、実DBコピーで旧5表の保全と契約・関連の生成を確認する。
- [x] 複数Hubの正常で新しい報告を一つの共通推定へ集約し、契約・枠の率と利用額ソースを一度ずつ採用するコードと自動テストを実装する。
- [x] 約2.9GBの実DBへストリーミングで適用し、適用後の保全を照合する。
- [x] headless UI と実データ再起動で共通契約・全体表示・履歴分離を確認する。画面の軽微な幅・空ラベル表示も修正後に再確認済み。

**解消結果:** 機能仕様第1.5節に、全サービス共通の契約・枠の対応とデータによる採否条件を定義しました。連携仕様第3.5節のCodex実GETは過去の個別検証の証跡として保持します。未観測の契約、全期間の帰属、収集範囲外の消費の有無はAPIだけでは証明できません。Claudeを含む全サービスに共通処理を適用し、モデル別プールも枠キーごとの参考値として扱います。枠別の推定値は合算しません。

**Grokの契約表示規則（2026-09-13）:** `provider=grok` と trim・lowercase した `accountEmail` をHub横断の契約キーとし、トークンの `accountKey` 変更では契約を分けません。メールが異なる場合は別契約、メール欠落行は契約を生成せず端末の取得情報として保持します。同メールの古い `unavailable` 行は同一契約、別メールの古い行は過去契約として保持し、複数メールの `ok` 行は n:n 関係で扱います。Grokにも全サービス共通の利用許容量推定を適用します。

**Claude組織識別の未解決（[Issue #34](https://github.com/nuitsjp/token-monitor-analytics/issues/34)）:** Claudeの組織識別に必要な情報の調査をIssueに登録しました。2026-09-13 の実HubではClaudeが `notConfigured` 6件と `sourceRateLimited` 1件で、`accountKey`・メールアドレス・組織識別子はありませんでした。上流の [`claudeStableIdentity(accountId,orgId,email)`](https://github.com/Javis603/token-monitor/blob/2f60827e3028d283969dd74cde5b3f5664220442/src/shared/providers/claude/limits.js#L717) は `accountId` を優先するため組織違いでも同じキーになり得る一方、利用量は組織指定API（[同ファイル L1066](https://github.com/Javis603/token-monitor/blob/2f60827e3028d283969dd74cde5b3f5664220442/src/shared/providers/claude/limits.js#L1066)）から取得します。現時点で誤統合は未確認です。Hub変更を前提にせず、正常な取得時の現行API応答に識別項目があるかを調査し、不足時は識別不能とします。このIssueは全サービス共通の推定実装をブロックしません。Claudeの正常な組織別実データの調査は未完了として残し、サービス個別の推定アルゴリズムは追加しません。

**解消条件:** API項目に基づく採用範囲と不可能なケースが実装され、Private・Workに分かれた同一契約の10 USD・5 USDの増分とΔp=10から共通推定1件・150 USDになること、片方のHubの欠測時に既存の比較を中断し、その端末を除外した新しい系列で参考値を推定できることを自動検証できること。

<a id="u3"></a>
### U3. 比較基準・観測・推定結果の管理

**前提:** 観測の採用、微小な測定時刻差の許容、累積額減少時の起点維持、計算区間・期間遷移は [推定仕様](doc/spec/functional-spec.md#estimation) の確定事項です。これらを実現する比較基準の管理と一括保存は [設計書 第5.2節](docs/architecture.md#52-比較基準と推定結果の管理) に対応します。旧Hub単位方式と新しい共通方式の比較基準・推定イベントはコード、自動テスト、実DB適用、headless Edgeで検証済みです（表示修正後の再確認も完了しています）。

- [x] 契約帰属・枠識別の成立条件に合わせ、観測データの同一性判定、順序管理、比較基準の更新責務を設計する。
- [x] 同じSSE通知に全データが含まれることを要求せず、不十分な端末を除外した論理利用額ソースとその契約の率が、前回比較点より新しい正常な報告としてそろった時だけ共通推定を確定する方針を合意する。
- [x] 切断、不正、未取得、消費率減少、古いデータ、リセット、契約関係・端末構成変更時に、関係する系列だけを中断し過去結果を保持する再開ルールを確定する。
- [x] 観測、契約関係、共通比較基準、推定結果、推定不可理由を一括永続化するトランザクション範囲と、旧方式履歴とのID・scope分離方針を策定する。
- [x] `shared_estimation_state`・`shared_estimation_events` を実装し、全Hubの通知をまたいだ比較点と根拠を保存するコードを自動テストする。
- [x] 保存失敗時の全体停止、正常なHubの未関係系列継続、再接続後の観測待ちを自動検証する。通常の再起動では保存済み比較基準を継続する回帰も追加した。
- [x] schema 8を実DB・Mockへ適用し、元8表の全行ハッシュ一致、既存結果・イベントの保持、修正後に記録した実受信入力の再生とcheckpointの完全一致、正常再起動後の継続を確認した。schema 7の完了判定を訂正し、欠測情報を仮定する再生を廃止した。検証件数・証跡・旧履歴の再計算限界は第1節に記録した。

**解消条件:** 通常受信、Hubをまたぐ分割受信、再受信、リセット、欠測、不正、未取得、保存失敗、再起動の各ケースで、採用する観測、更新担当、コミット対象、保持基準・結果が定義され、共通推定1件と旧方式履歴が混同されないことを実装・検証できること。

<a id="u4"></a>
### U4. 利用枠を観測間で識別する規則

**前提:** 利用枠の同一性と対象期間（リセット周期）は独立して判定します（Claude のモデル別枠、Codex の期間別枠等）。

識別に使える項目の制約は [連携仕様](doc/spec/interfaces.md#limit-fields)、対象期間の判定は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [x] 全providerの契約キーを `provider + accountKey` としHubをまたいで照合する。Grokだけは既合意の正規化した `accountEmail` で照合する。プラン名は契約の属性として保持し、同一性判定へ使わない。
- [x] アカウントと枠の構造キーを定義し、サービス名・枠種別・追加枠かどうかによる固定制限を設けず、取得値から採否を判定する。
- [x] limitIdがない枠では前後空白を除いたlabelも識別に使い、配列順は使わない。プラン属性・枠キー・期間・導出時の上限変更の扱いを定義する。
- [x] 識別不能な枠も取得値と理由を表示し、識別可能になった後は新しい比較を開始する。過去の推定結果は改変しない。
- [x] `device_contracts` と枠の対応を全Hubの観測へ適用し、同一契約の率を一度だけ採用するコードと自動テストを実装する。
- [x] 実DB適用後の契約・枠の件数と、headless UIでの一件化表示を確認する。画面修正後の最終再確認も完了した。

**解消条件:** 同一・別・識別不能の判定基準が具体例とともに整理され、未知のproviderを含む同一 `provider + accountKey` がHubをまたいで一契約として表示・推定されること、Grokの同一メールが一契約として表示されトークン変更で分割されないこと、別accountKeyが同名プランでも誤結合されないことを実装・検証できること。

<a id="u5"></a>
### U5. 端末変更と比較基準の分離

**現状:** 観測・端末はHubと `deviceId` の組み合わせで識別し、ID変更時は別端末として扱います（自動名寄せは行わず、手動関連付けは初期版対象外）。契約の識別は端末の識別と分離し、全providerの `provider + accountKey`（Grokは正規化メール）をHub横断キーとします。

識別規則は [設計書 第5.1節](docs/architecture.md#identity)、比較基準に適用する動作は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [x] Hubと `deviceId` を端末の保存キーとして維持し、端末ごとの利用額と測定日時を観測IDとともに保持する方針を合意する。
- [x] 契約とHub・端末・ツールの多対多関係を `device_contracts` で保持し、first/last観測IDを契約有効期間ではなく観測根拠として扱う方針を確定する。不明な時刻から関係や期間を生成しない。
- [x] 新しい契約関係・端末構成では関係する共通系列を開始し、不十分な端末を除外して参考値を推定する方針へ更新する。Cursorの代表報告端末だけが交替した場合は同じ論理ソースとして比較を継続する。無関係な共通系列は継続する。
- [x] 全観測と旧registryから端末・契約の関係を移行し、Hub横断の共通比較点へ反映するコードと自動テストを実装する。
- [x] 実DBへ適用し、実データ再起動後も端末・契約関係と共通比較点を保全できることを確認する。画面修正後の最終再確認も完了した。

**解消条件:** 同一IDの異なるHub、同一Hubの異なる端末、ID変更、同一契約の複数Hub・端末報告、関係ソース欠落の各パターンで、保存データと比較基準が正しく分離・接続されることを実装・検証できること。

<a id="u6"></a>
### U6. 履歴取得・日付・収集停止の制御

**前提:** [機能仕様 S2](doc/spec/functional-spec.md#s2) の取得契機・成功条件・通信再試行と、[S5](doc/spec/functional-spec.md#s5) の受信単位・停止前後の採否・保存失敗の併記は確定事項です。日付・合算表示は [第3節](doc/spec/functional-spec.md#history) に従います。処理担当、実行順序、トランザクション境界は以下と [設計書 第5.4節](docs/architecture.md#54-日次月次実績の保存)・[S2](docs/architecture.md#s2)・[S5](docs/architecture.md#s5) のとおり確定しました。

現在値機能の起動終了については、設定・収集状態の復元、終了前後の通知の採否、受信済みキューの処理と接続終了の順序を [設計書 S7](docs/architecture.md#s7) に具体化し、実装・自動検証しました。Hub個別の停止・再開操作と履歴取得との競合も修正し、以下の制御を実装・自動検証しました。

- [x] 端末タイムゾーン情報の欠落・不正時における当日分除外の安全な判定ルールと影響範囲を決定する。periodWindows.today.key を端末現地の今日とし、欠落・不正・タイムゾーン不正時は他ゾーンで代用せず当該端末の日次保存を見送る（月次は保存する）。影響範囲は当該端末の日次のみとし、他端末・月次・現在値・推定には及ぼさない。
- [x] 初回取得、再接続、定期取得、再試行の競合制御、キャンセル処理、古い応答の扱いを整理する。通信待機はキュー外で行い保存だけを共通キューへ直列化する。HubごとにGETを1件に制限し、停止・再開で世代番号を進めて遅着応答を破棄する。通信失敗は1時間間隔で再試行し、応答の骨格不正は再試行しない。再接続時は当日の定期取得が未完了なら再取得する。
- [x] 履歴レコード、取得完了フラグ、最終成功日時の保存担当コンポーネントおよびトランザクション境界を定める。取り込み制御が取得し永続化層の commitHistory が1回分の日次・月次行と history_fetch_state の成功日時を単一トランザクションで確定する。保存失敗は成功扱いにせず再試行の対象にもしない。
- [x] 停止要求受付、通信キャンセル、受信済みキュー処理、停止フラグ保存、画面通知の一連のシーケンスを明確化する。世代加算と進行中GETの中止・再試行解除、SSE収集ループの停止、受信済みキュー処理後の停止フラグ保存と停止表示の順序とする。受信途中の通信は中止し受付後の遅着応答は破棄する。
- [x] 停止フラグ保存失敗時や再起動、停止・再開が連続した場合の状態復元ルールを策定する。停止フラグはSQLiteに保存し再起動時に復元する。保存失敗時は保存失敗と併記する。連続した停止・再開は旧収集ループの終了を待って直列化し二重収集しない。

**修正と検証（2026-09-14、完了）:** 第1節のschema 10修正を適用し、202件の自動テスト、実HubのGET取得、実DB複製の移行検証、Windowsのブラウザー検証が成功しました。最初の追加テストはfixtureの構文誤り（Unexpected token）で失敗し、修正後は空のhubIdが200を返す境界不備を検出しました。API境界を修正して全件成功しています。

- [x] レビュー1〜8の保存停止、移行、再開、通信再試行、取得競合、停止表示、ページングを修正し回帰テストを追加する。
- [x] 完了前の照合で見つかった再接続の時刻境界を修正する。午前1時以後の再接続は、その日午前1時以後の成功だけを定期取得済みと判定する。00:30の初回成功後に02:00で再接続すると取得し、成功後の再接続では繰り返さないことを回帰テストで確認する。
- [x] レビュー9の履歴閲覧・二期間比較、途中集計・前日なし・判定不能表示を実装し、比較と全ページ取得を自動検証する。
- [x] 実Hub取得と、実DB・Mock複製の追加移行・データ保全を検証する。
- [x] Windowsブラウザーで履歴の絞り込み・更新・比較・ページングと、390px/1440pxの表示を確認する。利用者が今回限りの一時ヘッドレスEdgeを許可し、playwright-cliで検証しました。日次・月次それぞれ221件を3ページから取得し、111組の比較、100件ずつの追加表示、欠測時に差分を算出しない動作、途中集計・日付不明・前日データなしを確認しました。503応答時の既存表示保持、停止設定保存失敗時の収集停止・保存失敗の併記、再試行予定時刻も確認しました。両画面幅でページ全体の横はみ出しはなく、表は枠内で横スクロールできます。

ブラウザー接続では既存EdgeがECONNREFUSED、許可後のChromeもタイムアウト・HTTP 404となったため、許可された一時ヘッドレスEdgeで実施しました。初回の画面検証は「期間A」の要素指定不一致でTimeoutErrorとなり、検証スクリプトの指定を修正して全項目が成功しました。JavaScript例外は0件で、コンソールエラーは障害検証で意図的に返した503の1件です。

Web管理画面からの変更操作は [U7](#u7) に残ります。

<a id="u7"></a>
### U7. Hub 管理と秘密情報

**前提:** 管理操作の動作と検証条件は [機能仕様 S8](doc/spec/functional-spec.md#s8) に従います。初期版は利用者認証なしで運用し、Hub 接続設定・共有シークレットの管理契約を本項で定めます（利用者認証は U12）。

**ヒアリング状況:** 2026-09-13 に、起動時に一部 Hub の接続設定だけが不正な場合の起動・表示範囲を合意し、[機能仕様 S7](doc/spec/functional-spec.md#s7) に動作と検証条件、[設計書 S7](docs/architecture.md#s7) に担当と処理境界を反映しました。追加ヒアリング1-3〜1-5の設定入力・秘密情報保存・設定ファイル全体の障害方針を反映し、設定ファイルの形式・項目・検証条件と SQLite の Hub 登録情報との対応を具体化しました。設定例と権限設定スクリプトを作成し、設定境界の自動テストおよび Windows 実フォルダーのアクセス権を検証しました。Web からの設定更新は未完了です。

- [ ] Hub の新規登録、接続先変更、秘密情報更新における API 契約、バリデーション、結果表示を定義する。
- [x] 先行実装の設定ファイルの配置・権限設定手順と漏洩防止を具体化し、実装・検証する。
- [ ] Web 管理導入時の接続設定・秘密情報の永続化方法を定める。
- [ ] 管理 API における CSRF 対策方式と、要求受付・実行・通知の境界を明確化する。

**解消条件:** 正常操作、Hub 接続先での認証エラー、CSRF 検証失敗、入力値不正、保存失敗の各状況で、表示・保存・秘密情報保護の仕様が確定すること。

<a id="u8"></a>
### U8. 手動更新と履歴を保全する復旧

**前提:** [機能仕様 S9](doc/spec/functional-spec.md#s9) の動作と、[設計方針 第5節](docs/design-policy.md#5-データ保全と変更管理の境界) の保全・変更境界に従います。

- [ ] 更新元・更新対象の範囲、取得・適用・再起動のシーケンスと実行主体を決定する。
- [ ] アプリと保存データの互換性チェック、更新失敗時の残存整合性、手動復旧手順を策定する。
- [ ] 更新操作の入力検証・CSRF 対策、障害ログ出力、再起動後の状態表示を具体化する（利用者認証の追加は U12）。

**解消条件:** 更新中断・失敗時でも既存履歴を保全したまま復旧できる手順と検証条件が確立されること。

<a id="u9"></a>
### U9. 保存障害の確定点と影響範囲

**現状:** コミット前の書き込み失敗と、コミット完了後の読み出し・通知失敗では状態が異なります。要求する動作は [機能仕様 S6](doc/spec/functional-spec.md#s6)、保存の確定点は [設計書 第5.3節](docs/architecture.md#53-保存モデルと確定点) を参照します。

**ヒアリング状況:** 2026-09-13 に、原因や影響範囲が不明な保存障害、保存成功後の通知不達、稼働中の DB 参照不能について [機能仕様 S6](doc/spec/functional-spec.md#s6)、起動時の既存 DB 障害について [S7](doc/spec/functional-spec.md#s7) に、合意した動作と検証条件を反映しました。読み出し失敗と起動失敗の担当・処理境界は [設計書 S6](docs/architecture.md#s6)・[S7](docs/architecture.md#s7) に反映済みです。追加ヒアリング1-2の保存失敗時の日時表示も、機能仕様 S6 と設計書 S6 に反映しました。現在値の保存失敗・読み出し失敗・再接続は実装・自動検証済みです。以下の残件があるため U9 は未完了です。

- [x] 合意した通知不達時の動作に基づき、現在値の復帰処理の担当と通知契機を [設計書 第5.6節](docs/architecture.md#56-排他制御と通知シーケンス)・[S6](docs/architecture.md#s6) に具体化し、実装・自動検証する。
- [ ] 合意した保存停止範囲を前提に、共有 SQLite の異常発生時に継続可能な通信・閲覧・通知の境界を整理する（現在値・履歴取得は設計書 S6 とU6に具体化・自動検証済み。管理操作への適用は未完了）。
- [x] 現在値機能で保存失敗・コミット後の読み出し失敗を注入し、コミット前後の保存データ、配信する保持値、全 Hub の保存停止、再起動時の復元を自動検証する。
- [x] Chrome に保存失敗・DB 参照不能の状態を配信し、警告と数値・日時の保持を確認する（ブラウザ向け SSE 応答の差し替えによる表示検証）。
- [ ] 実際のディスク障害・読み取り権限異常時の運用を検証する。管理操作への停止範囲の適用も残る。

**解消条件:** コミット前失敗、コミット後失敗、DB アクセス不能の各ケースで、永続化と UI 表示の整合性が検証可能に整理されること。

<a id="u10"></a>
### U10. モック駆動開発を既定の進め方にするか

**現状:** [ADR 0002](docs/ard/0002-adopt-mock-driven-development.md) および [標準規約](docs/standards/mock-driven-development.md) に基づく検討項目です。

- [ ] 実画面による認識合わせを必須とする変更範囲と、既定プロセスとしての採否を判断する。

**解消条件:** 採否と適用範囲を決定し、文書方針および ADR に反映すること。

<a id="u11"></a>
### U11. モック開発の補助製品と配置

**現状:** [ADR 0003](docs/ard/0003-select-mock-tooling.md) および [ツール候補](docs/mock-tooling.md) に基づく検討項目です。

- [ ] 対象 UI、通信プロトコル（JSON/SSE）の再現範囲、MSW と同梱 Mock Hub の役割分担を整理する。
- [ ] 代表画面で保守負担と効果を評価し、導入構成（フルセットまたは最小構成）を判断する。

**解消条件:** 選定理由と構成を確定し、導入時は配置・実行コマンド・実機切り替え手順を策定すること。

<a id="u12"></a>
### U12. 利用者認証の後続実装

**前提:** 初期版は限定された安全な利用環境を想定し、ダッシュボード自体の利用者認証は初期版完了後のスコープとします。認証方式（Basic 認証等）や適用時期は後続で検討します（外部 Hub の共有シークレット認証は U7・連携仕様に従う）。

- [ ] 利用範囲に応じた認証方式、適用時期、Web 画面・静的アセット・JSON API・ブラウザ向け SSE・管理操作の保護範囲を定める。
- [ ] 方式決定後に認証を実装し、正常アクセス、認証失敗、資格情報保護、管理操作との整合を検証する。

**解消条件:** 認証方式と適用範囲を仕様・設計へ反映し、実装と必要な検証が完了すること（初期版の完了条件からは除外）。

<a id="unimplemented"></a>
## 5. 確定済みだが未完了の機能

現在値、従来のHub単位方式による採用範囲の推定、算出根拠の保存、旧方式の推定履歴閲覧は実装済みです。今回合意したHub横断の共通契約推定、schema 3〜6の契約・端末関連、schema 8の受信入力保存によるcheckpoint継続、全体の契約カード・推定表示・scope分離履歴もコード、自動テスト、実DB・Mock適用、実データ再起動確認まで実装し、schema 8の最終検証結果は第1節に記録しています。以下は初期版で提供予定の残りの機能です。未決事項は第4節、未検証範囲は U1・U7〜U9 で追跡します（利用者認証は U12 の後続作業）。

- [x] Hub横断の共通契約（provider + accountKey）を一件として推定し、除外後に採用する論理利用額ソースとその契約の新しい観測を待って共通状態・履歴を保存するコードと自動テストを実装する
- [x] `contracts`・`device_contracts`・`shared_estimation_state`・`shared_estimation_events` をschema 3へ追加し、旧5表の内容と旧方式履歴を保持して移行するコードを実装する（実DBコピーで移行・保全を確認済み）
- [x] `/api/state` の全体 `contracts`・`estimates`・`legacyEstimateCount`、Hub内の `contractIds`・`estimateIds`、`/api/estimates/history` の `scope=global|legacy` を実装する
- [x] 実DB適用後の保全、headless UI、実データ再起動による共通契約・全体表示・履歴分離の受け入れ確認

- [x] 日次・月次利用実績の GET 収集、過去データの補完、推移保存および比較機能（schema 10、U6の実装・検証完了。日次は前日以前の確定分のみ、月次は返却された各月を保存。当日分の転記・タイムゾーン変換・按分は行わない）
- [ ] Hub 管理画面（登録・一覧・個別停止・再開・接続設定変更）。設定ファイルによる複数 Hub の並行収集と起動時復元、停止・再開の内部操作は実装済み。
- [ ] 管理操作における CSRF 対策
- [ ] 手動アップデート機能および失敗時の復旧支援機能・運用手順

※ 各機能の実装後は [機能仕様 S1〜S9](doc/spec/functional-spec.md#scenarios) の検証条件、[設計書](docs/architecture.md#runtime) の処理・保存境界、および設計方針の受け入れ条件に基づき検証します。
