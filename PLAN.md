# 設計・開発計画と未決事項

本書を現在の到達点、作業の順序、未決事項・未検証事項、確定済みの未実装機能の正本とする。決定済みの仕様は [アーキテクチャ設計書](docs/architecture.md)、目標・制約は [設計方針](docs/design-policy.md) に記載する。

## 1. 現在の到達点

- 現在は設計段階である。[設計書](docs/architecture.md)に C4、識別・推定・保存・状態、重要シナリオ S1〜S9 があるが、[第4節](#open-items)の判断に依存するシナリオは未完であり、初期設計は完了していない。
- 実装・テスト・実行設定・CI・ローカルデータは [old/](old/README.md) に退避済みで、旧 Analytics は停止している。旧コードは一つの Hub の現在値受信・保存・認証付き表示までであり、現行実装の正本ではない。
- 文書整理後に実装・実機テストは再実行していない。確認済みの根拠と未検証範囲は [設計書 第9節](docs/architecture.md#9-品質確認リスクarc42-quality-requirements--risks) と U1 で区別する。

## 2. 不足を補う順序

| 順序 | 作業 | 完了の判断 |
| --- | --- | --- |
| 1 | U2・U4 の契約帰属と枠識別を、API の事実と具体例で整理する。 | 同一・別・識別不能を説明し、推定できる範囲とできない範囲が明確になる。 |
| 2 | U3・U5 の比較基準・結果保存、U6・U9 の並行制御・停止・保存障害を、重要シナリオへ反映する。 | 各シナリオの更新担当、コミット対象、失敗時に残る状態を説明できる。 |
| 3 | U7・U8 の管理と手動更新を具体化し、認証・保存・復旧の境界を揃える。 | 管理操作と更新の正常・失敗シナリオに検証条件がある。 |
| 4 | 設計が具体化した機能から実装・検証し、U1 の実機確認を進める。 | 設計方針の受け入れ条件を満たし、必要な実環境の確認が済む。 |

この順序は依存関係を示す。契約帰属の調査待ちでも、履歴日付や管理境界など独立した検討は進める。決定した仕様は設計書へ移し、未実装機能は[第5節](#unimplemented)で追跡する。

モック駆動開発の既定採用は U10、補助製品の採用は U11 で判断する。文書統合を採用・導入の完了とは扱わない。

## 3. 完了の判定

初期設計は、S1〜S9 の各シナリオについて [文書方針の5項目](docs/document-policy.md#4-判断の記録と初期設計の完了基準)を説明できた時点で完了する。製品の初期版は、実装と必要な検証が [設計方針の受け入れ条件](docs/design-policy.md#6-検証と製品の完了条件)を満たした時点で完了する。文書整理や未決事項の登録だけでは、どちらも完了しない。Linux 対応と端末の手動関連付けは後続範囲である。

<a id="open-items"></a>
## 4. 未決事項・未検証事項

U1〜U11 は、未決・未検証・未実装を混同せず、各項目の解消条件を満たすまで維持する。

| ID | 状態 | 依存・影響 |
| --- | --- | --- |
| [U1](#u1) 実 Hub と対応環境 | 未検証 | API 互換性、継続受信・再接続、Windows 実機。Linux は後続。 |
| [U2](#u2) 利用額の契約帰属 | 未決・要調査 | S1・S4 の契約別推定。現在値と契約へ配分しない履歴は独立。 |
| [U3](#u3) 比較基準と推定の確定 | 未決 | U2・U4・U5に依存。S1・S3・S4・S7 の推定・再起動。 |
| [U4](#u4) 利用枠の同一性 | 未決・要調査 | S1・S4 の推定と契約・枠別履歴。観測単位の保存・表示は独立。 |
| [U5](#u5) 端末別の比較基準 | 未決 | U2・U3・U4に依存。S1・S4・S7 の基準と履歴の分離。 |
| [U6](#u6) 履歴・並行制御・収集停止 | 未決 | S2・S5・S7。保存異常は S6・U9 にも影響。 |
| [U7](#u7) Hub 管理と認証境界 | 未決 | U6 の停止・再開に依存。S5・S8 と秘密情報・CSRF。 |
| [U8](#u8) 手動更新と復旧 | 未決 | S9 と互換性・履歴保全。データ消失リスク等は事前相談。 |
| [U9](#u9) 保存障害の確定点と影響範囲 | 未決・要検証 | S6・S7 と管理操作。共有 DB 障害の全 Hub への影響。 |
| [U10](#u10) モック駆動開発の既定採用 | 未決 | UI 変更の進め方と合意記録。内部変更・合意済み動作の修正は独立。 |
| [U11](#u11) モック開発の補助製品 | 未決・要確認 | 対象 UI・通信境界と開発用構成。プロセスの採否とは別に判断する。 |

<a id="u1"></a>
### U1. 実 Hub と対応環境の確認

**根拠:** 上流 `2f60827e3028d283969dd74cde5b3f5664220442` のソース調査と [Private Hub 単発取得](old/docs/reference/hub-private/README.md)。旧開発記録では Windows 上の初回 SSE の入力検証・一時 SQLite 保存まで成功したが、継続動作や現行実装を検証したものではない。

- [ ] 実 Hub の継続更新、切断・再接続、長時間稼働を確認する。初回 `snapshot` の成功からこれらの成功を推測しない。
- [ ] 調査基準と現在の上流 `c4182fd5bcdcd466f6bcd159026756323e084318` の差分から、使用する API・識別項目・履歴・利用枠の互換性を確認する。
- [ ] 複数 Hub、履歴収集、推定、管理、手動更新を実装した後、それぞれ Windows と実 Hub で必要な受け入れ条件を確認する。
- [ ] Linux の対応と OS 間の差異を後続で確認する。最初の Windows 完了判定には含めない。

**解消の条件:** 接続先の実装・リビジョン、実行環境、確認したシナリオ、結果と未実施範囲を残す。Mock Hub の成功を実機互換の根拠に置き換えない。

<a id="u2"></a>
### U2. 利用額を契約・利用枠へ帰属させる根拠

**確定していること:** 実績は端末・ツール等の累積 API 換算額を持つが、`accountKey` や契約 ID を保持しない。利用枠側のアカウントが一つ見えること、ツール名・プラン名が一致することだけでは結合しない。帰属不明なら推定不可とし、取得値は集計単位を明示して表示する。

- [ ] 採用する実績項目と集計期間を特定し、その利用額が単一契約の対象枠の消費に対応する根拠を確定する。
- [ ] 同じツールで複数契約・複数端末を使う場合、同一アカウントの契約・組織が変わる場合、モデル限定枠がある場合に、どこまで対応を証明できるか確認する。

**解消の条件:** 推定できる具体例とできない具体例を API 項目とともに示す。上流情報だけでは成立しない場合は、その不足を明記し、追加情報や要件変更が必要なら利用者へ提示する。根拠のない単一契約の仮定で埋めない。

<a id="u3"></a>
### U3. 比較基準・観測・推定結果の管理

**前提:** [設計書 第5節](docs/architecture.md#crosscutting)のとおり、同一 SSE の利用額と消費率を一組とし、同一対象期間の最初と最新の比較可能な観測を使う。推定結果は根拠とともに追記し、受信後の照合・計算・保存を直列化して保存後に成功を通知する。

- [ ] 契約帰属と枠識別の成立範囲に合わせ、観測の意味上の同一性と順序、最初・最新の比較基準を更新する責務を決める。退避した実装の表示データのハッシュをそのまま推定の識別キーとしない。
- [ ] 観測・比較基準・推定結果・推定不可の理由を一括して保存する範囲と、再受信時に結果を重複させない規則を決める。
- [ ] 切断中の欠測、消費率減少、古い値やリセット予定時刻の再出現の後、どこから比較を再開できるか決める。不明区間をまたぐ計算はしない。
- [ ] 同じ利用枠を複数端末が報告した場合、利用額と代表する消費率の対応、端末構成の変化、再起動後の継続を具体化する。

**解消の条件:** 通常受信、同一通知の再受信、リセット、欠測、保存失敗、再起動について、採用する二つの観測、更新担当、コミット対象、残る基準と結果を説明し、検証条件を定める。

<a id="u4"></a>
### U4. 利用枠を観測間で識別する規則

**前提:** 枠の同一性と対象期間は別に判定する。Claude には同じ `kind` のモデル別枠、Codex には同じ `limitId` の短時間・長時間枠があり、退避した実装は観測をまたぐ枠の結合をしていない。

- [ ] プロバイダーごとに、アカウント・契約情報、`kind`、`limitId`、`additional` 等のどの組で同じ利用枠と判定できるか確認する。
- [ ] 名前・プラン・組織・任意項目・配列順が変わる例を使い、継続できる場合と別枠または識別不能になる場合を定める。
- [ ] 特定不能な枠の保存・表示単位と、後の観測で識別できるようになった場合の扱いを決める。過去の推定結果は改変しない。

**解消の条件:** 単独項目では衝突する例を含め、同一・別・識別不能の判定を説明できる。`resetsAt` は枠識別後の期間判定に使う。

<a id="u5"></a>
### U5. 端末変更と比較基準の分離

**確定していること:** Hub と `deviceId` で端末を分離し、ID 変更は別端末とする。退避した実装の現在値保存では Hub・観測・端末を分離している。将来の手動関連付けは初期対象外。

- [ ] 端末ごとの比較基準と、同じ契約・利用枠を複数端末で使う場合の対応を保存モデルへ反映する。
- [ ] 旧端末が消え、新端末が現れた場合に、利用額を二重に含めず、別端末の観測を誤って継続しない条件を定める。

**解消の条件:** 同じ ID の異なる Hub、同一 Hub の異なる端末、ID 変更の例で、保存データと比較基準の分離を説明できる。端末 ID を手動で書き換える将来機能は含めない。

<a id="u6"></a>
### U6. 履歴取得・日付・収集停止の制御

**前提:** [設計書の S2・S5](docs/architecture.md#runtime)に、通信の独立実行と受信後の直列化、毎日午前1時の取得、通信失敗時の1時間間隔の再試行、端末現地日付の保持、停止受付前に受信完了したデータの処理を定めている。

- [ ] タイムゾーンが欠落または不正な端末で、日次の当日除外を安全に判定できる条件と、判定できない場合の保存・表示・継続範囲を決める。
- [ ] 初回、再接続、定期実行、再試行が重なる場合の GET の発行とキャンセル、古い応答の採否を決める。上流には共通の単調増加版番号がない。
- [ ] 履歴行と取得完了状態・最終成功日時をどの責務とトランザクションで保存するか決める。取得成功後の前日データ不在と通信失敗を分ける。
- [ ] 停止受付、全通信のキャンセル、受信済みキューの処理、停止設定の保存、画面通知の順序と担当を決める。管理操作を共通キューとどう対応させるか明示する。
- [ ] 停止設定を保存できない場合、操作途中で再起動した場合、停止と再開が重なった場合の設定・表示・再接続の扱いを決める。

**解消の条件:** 履歴取得中の新規 SSE、停止直前・直後の応答、日付境界、保存失敗、再起動の例ごとに、採用データ、担当、コミット対象、残る設定、停止・継続する処理を説明できる。

<a id="u7"></a>
### U7. Hub 管理と認証・秘密情報

**確定していること:** Web 画面で Hub を登録・一覧表示し、個別に停止・再開する。全 Web 経路に Basic 認証を適用し、重要操作に CSRF 対策を行う。退避した実装は `.env` の一つの Hub と参照専用 API だけで、同一 `HUB_ID` の URL 変更を拒否する。

- [ ] Hub の登録・接続先変更・秘密情報変更の操作契約、入力検証、成功・失敗時の表示を決める。情報源の変更と同一 Hub の接続先変更を混同しない。
- [ ] 接続設定と秘密情報の保存場所、退避した設定からの扱いを決める。秘密情報を観測履歴・ログ・レスポンスへ含めない。
- [ ] 管理 API の CSRF 対策と、管理操作の担当・保存・通知の境界を決める。

**解消の条件:** 正常操作、認証失敗、CSRF 拒否、不正入力、保存失敗について、情報源の識別と機密保護を保つ契約を示す。

<a id="u8"></a>
### U8. 手動更新と履歴を保全する復旧

**確定していること:** 明示操作で実行し、停止を許容する。自動更新・自動ロールバックをせず、DB 初期化・履歴改変を行わない。失敗の原因と手動復旧情報を提示する。

- [ ] 更新元と更新対象、取得・適用・再起動の担当と順序を決める。
- [ ] アプリケーションと保存データの互換性確認、途中失敗時に残るファイルとデータ、利用者が行う復旧手順を決める。
- [ ] 更新操作の認証・CSRF、失敗のログと再起動後の表示を具体化する。

**解消の条件:** 更新の途中で失敗する具体例に対し、履歴を保持したまま復旧できる手順と検証条件を示す。

<a id="u9"></a>
### U9. 保存確定後の失敗と障害の影響範囲

**確認した実装:** [store.js](old/src/store.js) はトランザクションをコミットした後に現在値を読み出す。[app.js](old/src/app.js) は保存関数の例外を保存異常として扱う。このため、コミット前の書き込み失敗と、コミット済みだが結果を読み出せない失敗は分けて説明する必要がある。

- [ ] コミット後の読み出し・通知が失敗した場合、永続化済みデータ、画面が保持する値、保存・配信の状態、再起動後の復元を定義する。
- [ ] DB の読み出し自体ができない場合に、参照可能な範囲と停止する機能を明確にする。
- [ ] 複数 Hub で共有する DB の異常について、停止する書き込みの範囲と、継続する通信・閲覧・状態通知を具体化する。

**解消の条件:** コミット前、コミット後、DB 参照不能の失敗を分けて、永続状態と表示状態を検証する。現在のコードの存在だけで全ケースを設計・検証済みとせず、失敗時は常にロールバック済みと説明しない。

<a id="u10"></a>
### U10. モック駆動開発を既定の進め方にするか

**根拠:** [ADR 0002](docs/ard/0002-adopt-mock-driven-development.md) と[実施ルール案](docs/standards/mock-driven-development.md)。キットの文書統合は、プロセスを既定採用する合意ではない。

- [ ] 動く UI による認識合わせが必要な変更への既定適用と、対象範囲を判断する。

**解消の条件:** 採否・範囲を利用者が明示し、文書方針と ADR に記録する。個別作業での選択やモック画面の承認を、全体の採用合意へ広げない。

<a id="u11"></a>
### U11. モック開発の補助製品と配置

**根拠:** [ADR 0003](docs/ard/0003-select-mock-tooling.md) と[製品候補](docs/mock-tooling.md)。現行の UI 実装・実行設定はなく、Storybook・MSW・Playwright の適合性は未確認である。

- [ ] 対象 UI と JSON・SSE の再現範囲、MSW と同梱する Mock Hub の役割を確認する。
- [ ] 代表画面で表示・応答再現・操作検証と保守負担を評価し、Storybook を含む案と最小案の採否を判断する。

**解消の条件:** 製品ごとの採否と根拠を明示し、導入する場合は配置・実行設定・モック接続と実接続の確認手順を確定する。旧 Playwright CLI や Mock Hub の存在を新構成の導入・実機互換性の根拠にしない。

<a id="unimplemented"></a>
## 5. 確定済みだが未実装の機能

これらは要件の未決を意味しない。必要な設計判断は上の ID で追跡する。

- [ ] 契約・利用枠別の推定、比較基準・算出結果の保存、推定履歴の閲覧。
- [ ] 日次・月次履歴の GET 収集、補完、保存、閲覧・比較。
- [ ] 複数 Hub の並行収集と登録・一覧・停止・再開・設定復元。
- [ ] 管理操作の CSRF 対策。
- [ ] 手動更新と失敗時の復旧機能・運用手順。

実装後は [設計書の S1〜S9](docs/architecture.md#runtime) と設計方針の受け入れ条件を検証する。Linux と端末の手動関連付けは初期版の対象へ追加せず、既に決まっている後続範囲として扱う。
