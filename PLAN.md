# アーキテクチャ設計計画

## 目的と範囲

Token Monitor Analytics の初期アーキテクチャを設計し、実装に必要な責務、識別・保存ルール、状態、処理順序、および検証方針を明確にする。

目標・制約・品質要求は [設計・実装ガードレール](docs/design-policy.md)、設計方法と文書の役割は [アーキテクチャ設計の進め方](docs/architecture-process.md) に従う。arc42 を文書の骨格とし、C4 の構造図と、4+1 の Process・Scenarios の観点を使う。

本計画の対象は調査、設計、設計文書の作成とレビューである。アプリケーションや Mock Hub の実装、実装後の動作試験、配布・更新の実行は含めない。実装時に行う検証は、その方法と期待結果を設計する。

## 成果物と進捗の扱い

| 成果物 | 内容 |
| --- | --- |
| `CONTEXT.md` | 確定した業務用語の定義。API 項目、識別キー、遷移条件などの処理仕様は含めない。 |
| `docs/architecture.md` | 調査根拠、C4 図、データ設計、状態とシナリオ、並行制御、認証・運用の設計、要求と検証方法の対応、未確認事項。 |
| `docs/adr/NNNN-*.md` | 記録基準を満たす設計判断が生じた場合に作成する ADR。 |
| `PLAN.md` | 作業の進捗と、完了した成果物への参照。仕様の正本にはしない。 |

チェックは成果物と確認結果が揃ってから更新する。用語集や設計書は内容ができた時点で作成し、空の ADR や文書を先に揃えない。

2026-09-12: 手順 1 に着手した。上流を `upstream/token-monitor` にサブモジュール登録し、v0.56.0（`2f60827e3028d283969dd74cde5b3f5664220442`）のソースを調査した。[用語集](CONTEXT.md) と [設計書](docs/architecture.md) に確認結果を記録した。Private Hub の GET と SSE 初回 snapshot を取得し、[実データ資料](docs/reference/hub-private/README.md) に保存した。U2・U3 の初期方針を [設計書 第 3.3 節](docs/architecture.md#33-推定できる条件と単位) に記録した。U4 の対象期間判定方針は合意済みとして [設計書 第 3.4 節](docs/architecture.md#34-リセットと欠測) を参照する。U5 のログ複製の扱いは合意済みとして [設計書 第 3.2 節](docs/architecture.md#32-再受信と二重計上防止) を参照する。端末 ID 変更時の初期方針は [設計書 第 3.1 節](docs/architecture.md#31-情報源と対象の分離) に、過去データを手動で新しい `deviceId` に結び付ける将来要件は [設計方針 第 6 節](docs/design-policy.md#6-事前相談と将来要件) に記録した。対象サービス・プランと各枠の識別詳細、および U5 の同じ端末 ID の再利用・収集範囲変更時の実績継続性が残るため、手順 1 全体は完了していない。手順 2～4 は未着手である。

## 1. Hub API の確認と識別ルールの整理

### 作業

- [x] [README](README.md) が参照する上流 Hub の仕様・ソースと対象バージョンを確認し、参照先とリビジョンを設計書に記録する。
- [x] 実 Hub の参照可否と利用可能なデータを確認する。Private Hub の GET と SSE 初回 snapshot を取得し、[実データ資料](docs/reference/hub-private/README.md) に保存した。認証情報は文書に記録しない。取得範囲と未確認事項を分けて記録する。
- [x] SSE と GET について、認証、データ形式、識別子、時刻・順序情報、再接続、履歴取得範囲、リセットの表現を確認する。イベント ID や再送機能の存在を仮定しない。
- [x] Hub、契約、利用枠、対象期間、観測・実績、増分、推定、補完、リセット、推定不可の意味を具体例で確認し、確定した用語を `CONTEXT.md` に記録する。
- [ ] 同じ実績の判定、Hub・契約・利用枠ごとの分離、観測の対応付け、推定不可の条件を整理する。算出に必要な値と単位も確認する。
- [x] 保存が必要な情報と Hub から再取得できる情報を整理し、再起動・欠測・リセット時に保てる保証と限界を明らかにする。

確認した API と制約は [設計書 第 2 節](docs/architecture.md#2-hub-api-の確認済み事実)、識別・推定の原則は [第 3 節](docs/architecture.md#3-識別推定について確定できる原則)、保存対象と補完の限界は [第 4 節](docs/architecture.md#4-保存が必要な情報と再取得の限界) に記録した。残る確認事項と確定できない設計は [第 5 節](docs/architecture.md#5-未確認事項と設計への影響) に記載した。API のチェック完了は固定リビジョンのソース確認を示し、実 Hub のチェック完了は保存資料に記録した GET と SSE 初回 snapshot の取得範囲を示す。いずれも実機検証全体の完了を意味しない。

### 完了条件

識別と推定の判断に使う情報を、確認した API 仕様・データへ対応付けて説明できる。未確認事項には、影響する設計、確認方法、確認できるまで確定できない事項を記録する。

**成果物:** `CONTEXT.md`、`docs/architecture.md` の調査根拠・識別ルール・未確認事項。

## 2. システム境界の定義（C4 Context / Container）

### 作業

- [ ] Context 図で、利用者、複数の上流 Hub、Analytics の責任範囲と外部連携を示す。
- [ ] Container 図で、Node.js アプリ、ローカル SQLite、ブラウザ側の実行部分の境界と通信を示す。単一常駐プロセスという制約に従い、保存先と実行部分を区別する。
- [ ] Hub 向け SSE・GET、Web 画面・API・ブラウザ向け SSE の経路を整理し、認証・入力検証・CSRF 対策を適用する境界を示す。
- [ ] Windows / Linux の配置、起動・終了、設定・認証情報・DB の置き場所について設計上必要な事項を整理する。アクセス制御と通信経路の暗号化を区別する。

### 完了条件

システムの内外、実行・保存の境界、通信と認証の経路が図と説明で一致している。単一ユーザー、単一常駐プロセス、ローカル SQLite という前提に矛盾がない。

**成果物:** `docs/architecture.md` の Context 図・Container 図・境界と配置の説明。

## 3. 内部責務と処理フローの設計

手順 1 の識別ルールと手順 2 の境界を入力とする。シナリオを検討して必要な責務を Component 図へ反映し、矛盾が見つかった場合は識別ルールや境界を見直す。部品数や旧実装の分割を先に固定しない。

### データ・状態・並行制御

- [ ] 受信、実績補完、推定、保存、画面配信、Hub 管理、手動更新の責任と依存関係を整理し、主要な Component 図を作成する。
- [ ] SQLite の保存対象、主キー・一意性制約、観測と推定結果の関係を定義する。算出時点の推定結果を保持し、再受信・再起動でも二重計上しない保存ルールを設計する。
- [ ] Hub の接続状態、保存処理の状態、推定対象の算出可否を分け、各状態の保持場所、更新処理、遷移条件を定義する。同時に成立する状態を単一の排他的な状態にまとめない。
- [ ] 状態の組み合わせと表示方法を定義する。観測・算出時点を扱い、過去の保存値と現在の状態を区別する。
- [ ] 並行実行する処理と順序を制御する処理を特定し、排他制御の範囲、SQLite トランザクション境界、保存確定と通知の順序を決める。方式は必要な整合性を明らかにしてから選ぶ。
- [ ] Hub の登録・一覧・個別停止、アプリの起動・終了・再起動、手動更新について、進行中処理と保存データの扱いを設計する。

### 主要シナリオ

各シナリオについて、入力・事前状態、処理順序、保存結果、表示、継続・停止する処理、ログ、検証時の期待結果を記録する。複雑な相互作用はシーケンス図、状態と遷移条件は表または状態遷移図で表す。

| シナリオ | 確認する設計上の論点 |
| --- | --- |
| 複数 Hub の通常受信・推定・保存・配信 | Hub・契約・利用枠の混同を防ぎ、保存済みの結果を正しく配信する。 |
| 切断・再接続・実績補完 | 補完中の新規受信と重複受信を含め、購読と GET の順序、欠落と重複の扱い、復元可能な範囲を定義する。 |
| リセット・欠測・対応関係が不明な観測 | 異なる期間の増分を混在させず、推定不可の理由と扱いを定義する。 |
| Hub の個別停止と実行中処理の競合 | 遅れて到着する受信・補完結果の扱いと、他 Hub の処理継続を定義する。 |
| 一部 Hub の不正データ・非互換 | 影響を受ける処理を特定し、他 Hub の通信と安全な履歴閲覧を維持する。 |
| 保存失敗・書き込み不整合 | 危険な書き込みを止め、誤った成功通知を防ぐ。安全な読み取りの範囲と原因を追跡する情報を定義する。 |
| プロセスの停止・再起動 | 保存確定前後に停止した場合の残存データと、再取得・再処理による整合性を確認する。 |
| 認証・管理操作 | Web 画面・API・SSE の保護、入力検証、CSRF 対策、機密情報の非露出を確認する。 |
| 手動更新の成功・失敗 | 更新手順、処理停止、履歴保全、失敗通知、手動復旧に必要な情報を定義する。 |

- [ ] 上記の各シナリオを設計書に記録し、構造・識別ルール・状態・並行制御の整合性を確認する。

### 完了条件

主要シナリオごとに「何を同一と判定し、誰が状態を更新し、いつ保存が確定し、失敗時に何が残るか」を説明できる。実行時の処理と静的な責務分担が一致し、既存方針の保証範囲を超える仕組みを追加していない。

**成果物:** `docs/architecture.md` の Component 図・データ設計・状態定義・処理フロー・運用設計。

## 4. 設計判断の記録と要件照合

### 作業

- [ ] 設計中に生じた判断を整理する。変更負担が大きく、採用理由が自明でなく、実際のトレードオフがある判断だけを ADR に記録する。
- [ ] 仕組み追加に該当する判断には、設計方針第 2 節の 4 点説明を残す。合意済みの要件・リスク受容を変更する必要が判明した場合は、影響を整理して相談する。
- [ ] 設計方針の各要求について、設計書の該当箇所、検証方法、期待結果を対応付ける。
- [ ] Mock Hub で再現するケースと実 Hub で確認する互換性を分け、単体・結合・ブラウザ・Windows / Linux の各検証で確認する内容を定める。必要なデータや環境、未実施項目を明記する。
- [ ] 用語、図、識別キー、状態、トランザクション、通知、障害時動作を横断してレビューし、指摘を「必須修正」「任意改善」「対象外」に分類する。
- [ ] 必須修正を解消し、設計を左右する未確認事項の影響と扱いを確定する。取得できない情報を推測で埋めず、既知のリスクとして残せるかは設計方針と照合する。
- [ ] 成果物への参照と完了した項目を本計画に反映する。動作試験を計画したことと、試験に成功したことを区別する。

### 完了条件

[初期設計の完了基準](docs/architecture-process.md#5-初期設計の完了基準)を満たし、要求と設計・検証方法の対応が確認できる。必須修正が残っていない。未確認事項を残す場合は、影響と扱いが明記され、既存方針の保証やリスク受容と矛盾しない。

**成果物:** 必要な ADR、`docs/architecture.md` の要求対応・検証方針・未確認事項、更新済みの本計画。

## 依存関係と作業分担

手順 1 で確認した識別情報を前提に、手順 2・3 の具体的な設計を確定する。影響を受けない配置や認証の調査は並行して進められる。手順 4 の判断記録は設計中から行い、最終的な要件照合は全体の設計が揃ってから行う。

要件整理、設計判断、レビュー、統合は主担当が行う。上流仕様の事実確認や文書の対応確認など、目的・範囲・完了条件を明確にできる独立作業は委譲し、結果を根拠とともに確認して統合する。

## 着手時に確認する情報

| 確認事項 | 不明な場合の扱い |
| --- | --- |
| 対象 Hub のバージョンと API・データ形式 | 上流の仕様・ソースを調査し、対象を特定する。識別・補完に関わる事実が分からなければ、その部分の設計を確定しない。 |
| 実 Hub への参照手段と実データ | 接続先などユーザーにしか提供できない情報が必要な場合は、その情報を確認する。取得できた実データの範囲と、未取得・未確認事項を記録する。 |
| 実装検証に使える Windows / Linux とブラウザの環境 | 検証方針に必要な環境を明記し、利用可能であることや動作成功を仮定しない。 |

接続 Hub 数、保持量、応答時間の数値目標は未指定である。独自の上限値や早期最適化を計画へ追加せず、具体的な判断に必要になった場合に確認する。
