# 設計・開発計画と未決事項

本書は、開発の到達点、作業順序、未決・未検証事項、および確定済みの未実装機能に関する正本です。決定済みの動作は [機能仕様](doc/spec/functional-spec.md)、API の事実は [連携仕様](doc/spec/interfaces.md)、構造・責務・保存境界は [設計書](docs/architecture.md)、製品要件は [設計方針](docs/design-policy.md) を参照してください。

## 1. 現在の到達点

- **設計段階**: [機能仕様](doc/spec/functional-spec.md#scenarios) に S1〜S9 の動作・検証条件、[設計書](docs/architecture.md) に C4 モデル、識別・比較基準・保存・状態の責務を記載しています。[未決事項](#open-items) に依存するシナリオが残るため初期設計は未完了です。
- **要件・設計の統合**: 2026-09-13 に `doc/spec/` を仕様の正本として、旧文書の推定条件・観測の鮮度・履歴取得成功・収集停止・日付表示・Hub API 制約を補完しました。性能方針・通信の保証範囲・必須修正の完了条件も設計方針へ反映しました。旧実装の具体値は新規採用せず、U1〜U11 の未決・未検証状態を維持しています。
- **実装の退避**: 過去の実装・テスト等は [old/](old/README.md) に退避済みです（旧実装は単一 Hub の現在値受信・保存・表示までであり現行仕様とは異なります）。
- **実機検証の状況**: 設計再整理後の実機テストは未実施です。確認根拠と未検証範囲は [設計書 第9節](docs/architecture.md#quality-and-risks) および [U1](#u1) で管理します。

## 2. 作業の進め方と順序

| 順序 | 作業内容 | 完了条件 |
| :---: | --- | --- |
| 1 | U2・U4 の契約帰属と利用枠の識別規則を整理する | 同一・別・識別不能の判定基準が確定し、推定可能な範囲が明確になること |
| 2 | U3・U5 の比較基準・結果保存、U6・U9 の並行制御・停止・保存障害処理をシナリオへ反映する | 各シナリオの更新担当、コミット対象、異常時の保持状態が明確になること |
| 3 | U7・U8 の Hub 管理と手動更新手順を具体化し、境界を整合させる | 管理操作および手動更新の正常系・異常系シナリオに検証条件が定まること |
| 4 | 設計確定機能から順次実装・検証を進め、U1 の実機確認を行う | 設計方針の受け入れ条件を満たし、実機動作を確認できること |

※ 論理的な依存関係を示すものであり、独立した検討は並行して進めます。確定した動作・API 仕様は `doc/spec/`、実現する責務・保存境界は設計書へ反映し、未実装機能は [第5節](#unimplemented) で追跡します。モック駆動開発の採否は U10、補助ツールの選定は U11 で判断します。

## 3. 完了の判定

- **初期設計の完了**: S1〜S9 の各シナリオについて [文書方針の5項目](docs/document-policy.md#initial-design-completion) を具体的に説明できた時点。
- **製品初期版の完了**: 実装および検証が [設計方針の受け入れ条件](docs/design-policy.md#product-completion) を満たした時点（Linux 対応および端末手動関連付けは初期版完了後の後続スコープ）。

<a id="open-items"></a>
## 4. 未決事項・未検証事項

未決・未検証の課題は以下の ID で管理し、解消条件が満たされるまで追跡します。

| ID | 状態 | 依存・影響範囲 |
| --- | :---: | --- |
| [U1](#u1) 実 Hub と対応環境 | 未検証 | API 互換性、継続受信・再接続、Windows 実機検証（Linux は後続） |
| [U2](#u2) 利用額の契約帰属 | 未決・要調査 | S1・S4（契約別推定）。現在値表示や契約非依存の履歴収集とは独立 |
| [U3](#u3) 比較基準と推定の確定 | 未決 | U2・U4・U5 に依存。S1・S3・S4・S7（推定・再起動） |
| [U4](#u4) 利用枠の同一性 | 未決・要調査 | S1・S4（推定および契約・枠別履歴）。観測単位の保存・表示とは独立 |
| [U5](#u5) 端末別の比較基準 | 未決 | U2・U3・U4 に依存。S1・S4・S7（基準保持と端末分離） |
| [U6](#u6) 履歴・並行制御・収集停止 | 未決 | S2・S5・S7。保存異常処理は S6・U9 にも影響 |
| [U7](#u7) Hub 管理と認証境界 | 未決 | U6 の停止・再開に依存。S5・S8（秘密情報保護・CSRF 対策） |
| [U8](#u8) 手動更新と復旧 | 未決 | S9（互換性確認・履歴保全）。データ消失リスク等は事前合意を要する |
| [U9](#u9) 保存障害の確定点と影響範囲 | 未決・要検証 | S6・S7 および管理操作。共有 DB 障害時の他 Hub への影響分離 |
| [U10](#u10) モック駆動開発の既定採用 | 未決 | UI 変更フローと合意記録。内部ロジックや既存仕様修正とは独立 |
| [U11](#u11) モック開発の補助製品 | 未決・要確認 | 対象 UI・通信境界と開発構成。プロセスの採否とは別に判断 |

<a id="u1"></a>
### U1. 実 Hub と対応環境の確認

**現状:** 上流コミット `2f60827e` の調査および [Private Hub 単発ログ](old/docs/reference/hub-private/README.md) に基づく。旧実装では Windows 上での初回受信・保存まで確認済みですが、現行仕様での長時間稼働検証は未実施です。

調査した API の項目・制約・確認範囲は [連携仕様](doc/spec/interfaces.md) を参照します。

- [ ] 実 Hub に対する継続受信、切断・自動再接続、長時間稼働を実機検証する。
- [ ] 調査基準コミットと文書の比較対象版（`c4182fd5`）の差分を精査し、API・識別項目・履歴・利用枠の互換性を確認する。
- [ ] 各機能の実装後、Windows 実機で受け入れ条件を検証する（Linux 対応は後続）。

**解消条件:** 実機環境での検証シナリオと結果が記録され、未検証範囲が明確になっていること（Mock Hub のみで代替しない）。

<a id="u2"></a>
### U2. 利用額を契約・利用枠へ帰属させる根拠

**現状:** Hub からの実績データは端末・ツール別の累積 API 換算額を含みますが、契約 ID 等は保持しません。名称の一致のみによる機械的な結合は行わず、帰属が特定できない場合は「推定不可」とします（集計単位を明記して表示）。

取得項目は [連携仕様](doc/spec/interfaces.md#observation-fields)、成立条件は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [ ] 採用する実績項目と集計期間を特定し、利用額増分が特定契約の利用枠消費に対応する根拠を確立する。
- [ ] 同一ツールの複数契約・複数端末利用、契約変更、モデル限定枠など各ケースの対応可否を整理する。

**解消条件:** API 項目に基づき推定可能なケースと不可能なケースの具体例が明確化されていること。

<a id="u3"></a>
### U3. 比較基準・観測・推定結果の管理

**前提:** 観測の採用、微小な測定時刻差の許容、累積額減少時の起点維持、計算区間・期間遷移は [推定仕様](doc/spec/functional-spec.md#estimation) の確定事項です。これらを実現する比較基準の管理と一括保存は [設計書 第5.2節](docs/architecture.md#52-比較基準と推定結果の管理) に対応します。

- [ ] 契約帰属・枠識別の成立条件に合わせ、観測データの同一性判定、順序管理、比較基準の更新責務を定義する。
- [ ] 観測、比較基準、推定結果、推定不可理由を一括永続化するトランザクション範囲と重複算出防止ルールを策定する。
- [ ] 切断に伴う欠測、消費率減少、古いデータの再受信、リセット予定時刻更新時の比較再開ルールを明確化する。
- [ ] 同一利用枠を複数端末が報告する場合の扱い、端末構成変化時の挙動、再起動後の継続処理を具体化する。

**解消条件:** 通常受信、再受信、リセット、欠測、保存失敗、再起動の各ケースで、採用する観測、更新担当、コミット対象、保持基準・結果が定義され、検証条件が定まること。

<a id="u4"></a>
### U4. 利用枠を観測間で識別する規則

**前提:** 利用枠の同一性と対象期間（リセット周期）は独立して判定します（Claude のモデル別枠、Codex の期間別枠等）。

識別に使える項目の制約は [連携仕様](doc/spec/interfaces.md#limit-fields)、対象期間の判定は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [ ] 各プロバイダーで、アカウント情報、`kind`、`limitId`、`additional` 等のどの組み合わせで同一枠と判定できるか特定する。
- [ ] 名称、プラン、組織、任意フィールドの変更や配列順の変動時の同一枠／別枠判定ルールを定義する。
- [ ] 識別不能な枠の保存・表示単位と、後続観測で識別可能になった場合の扱いを定める（過去の推定結果は遡及改変しない）。

**解消条件:** 同一・別・識別不能の判定基準が具体例とともに整理されていること。

<a id="u5"></a>
### U5. 端末変更と比較基準の分離

**現状:** Hub と `deviceId` の組み合わせで端末を識別し、ID 変更時は別端末として扱います（自動名寄せは行わず、手動関連付けは初期版対象外）。

識別規則は [設計書 第5.1節](docs/architecture.md#identity)、比較基準に適用する動作は [推定仕様](doc/spec/functional-spec.md#estimation) を参照します。

- [ ] 端末ごとの比較基準保持と、単一契約・利用枠を複数端末で利用する場合のデータ構造・保存モデルを定義する。
- [ ] 旧端末の停止と新端末の追加時における二重計上や誤った継続処理を防止する条件を策定する。

**解消条件:** 同一 ID の異なる Hub、同一 Hub の異なる端末、ID 変更の各パターンで、保存データと比較基準が正しく分離されることが説明できること。

<a id="u6"></a>
### U6. 履歴取得・日付・収集停止の制御

**前提:** [機能仕様 S2](doc/spec/functional-spec.md#s2) の取得契機・成功条件・通信再試行と、[S5](doc/spec/functional-spec.md#s5) の受信単位・停止前後の採否・保存失敗の併記は確定事項です。日付・合算表示は [第3節](doc/spec/functional-spec.md#history) に従います。今回の統合で、以下の担当・順序・トランザクション等が確定したわけではありません。

- [ ] 端末タイムゾーン情報の欠落・不正時における当日分除外の安全な判定ルールと影響範囲を決定する。
- [ ] 初回取得、再接続、定期取得、再試行の競合制御、キャンセル処理、古い応答の扱いを整理する。
- [ ] 履歴レコード、取得完了フラグ、最終成功日時の保存担当コンポーネントおよびトランザクション境界を定める。
- [ ] 停止要求受付、通信キャンセル、受信済みキュー処理、停止フラグ保存、画面通知の一連のシーケンスを明確化する。
- [ ] 停止フラグ保存失敗時や再起動、停止・再開が連続した場合の状態復元ルールを策定する。

**解消条件:** 履歴取得中の SSE 受信、停止前後の処理、日付境界、保存失敗、再起動の各ケースで、採用データ、担当、コミット対象、維持・停止される処理が明確になること。

<a id="u7"></a>
### U7. Hub 管理と認証・秘密情報

**前提:** 管理操作の動作と検証条件は [機能仕様 S8](doc/spec/functional-spec.md#s8) に従います。Web 全経路の Basic 認証と Hub 連携の共有シークレット認証を分け、以下で管理操作の具体契約を定めます。

- [ ] Hub の新規登録、接続先変更、秘密情報更新における API 契約、バリデーション、結果表示を定義する。
- [ ] 接続設定および秘密情報の安全な永続化方法を定める（ログや画面への漏洩防止を含む）。
- [ ] 管理 API における CSRF 対策方式と、要求受付・実行・通知の境界を明確化する。

**解消条件:** 正常操作、認証エラー、CSRF 検証失敗、入力値不正、保存失敗の各状況で安全性が担保される仕様が確定すること。

<a id="u8"></a>
### U8. 手動更新と履歴を保全する復旧

**前提:** [機能仕様 S9](doc/spec/functional-spec.md#s9) の動作と、[設計方針 第5節](docs/design-policy.md#5-データ保全と変更管理の境界) の保全・変更境界に従います。

- [ ] 更新元・更新対象の範囲、取得・適用・再起動のシーケンスと実行主体を決定する。
- [ ] アプリと保存データの互換性チェック、更新失敗時の残存整合性、手動復旧手順を策定する。
- [ ] 更新操作の認証・CSRF 対策、障害ログ出力、再起動後の状態表示を具体化する。

**解消条件:** 更新中断・失敗時でも既存履歴を保全したまま復旧できる手順と検証条件が確立されること。

<a id="u9"></a>
### U9. 保存障害の確定点と影響範囲

**現状:** コミット前の書き込み失敗と、コミット完了後の読み出し・通知失敗では状態が異なります。

要求する動作は [機能仕様 S6](doc/spec/functional-spec.md#s6)、保存の確定点は [設計書 第5.3節](docs/architecture.md#53-保存モデルと確定点) を参照します。

- [ ] コミット完了後の読み出し・通知障害時における永続化済みデータ、UI 保持値、配信状態、再起動時の復元動作を定義する。
- [ ] DB 接続・読み出し不能時の表示および停止範囲を明確化する。
- [ ] 共有 SQLite の異常発生時における書き込み停止範囲と、継続可能な通信・閲覧・通知の境界を整理する。

**解消条件:** コミット前失敗、コミット後失敗、DB アクセス不能の各ケースで、永続化と UI 表示の整合性が検証可能に整理されること。

<a id="u10"></a>
### U10. モック駆動開発を既定の進め方にするか

**現状:** [ADR 0002](docs/ard/0002-adopt-mock-driven-development.md) および [標準規約](docs/standards/mock-driven-development.md) に基づく検討項目です。

- [ ] 実画面による認識合わせを必須とする変更範囲と、既定プロセスとしての採否を判断する。

**解消条件:** 採否と適用範囲を決定し、文書方針および ADR に反映すること。

<a id="u11"></a>
### U11. モック開発の補助製品と配置

**現状:** [ADR 0003](docs/ard/0003-select-mock-tooling.md) および [ツール候補](docs/mock-tooling.md) に基づく検討項目です。

- [ ] 対象 UI、通信プロトコル（JSON/SSE）の再現範囲、MSW と同梱 Mock Hub の役割分担を整理する。
- [ ] 代表画面で保守負担と効果を評価し、導入構成（フルセットまたは最小構成）を判断する。

**解消条件:** 選定理由と構成を確定し、導入時は配置・実行コマンド・実機切り替え手順を策定すること。

<a id="unimplemented"></a>
## 5. 確定済みだが未実装の機能

以下の機能は仕様として確定しており、設計・実装フェーズで順次対応します（設計論点は第4節の各 ID で管理）。

- [ ] 契約・利用枠別の利用許容量推定、比較基準・算出根拠の保存、推定履歴の閲覧機能
- [ ] 日次・月次利用実績の GET 収集、過去データの補完、推移保存および比較機能
- [ ] 複数 Hub の並行収集、管理画面（登録・一覧・個別停止・再開・設定復元）
- [ ] 管理操作における CSRF 対策
- [ ] 手動アップデート機能および失敗時の復旧支援機能・運用手順

※ 各機能の実装後は [機能仕様 S1〜S9](doc/spec/functional-spec.md#scenarios) の検証条件、[設計書](docs/architecture.md#runtime) の処理・保存境界、および設計方針の受け入れ条件に基づき検証します。
