# 設計・開発計画と未決事項

本書は、開発の到達点、作業順序、未決・未検証事項、および確定済みの未実装機能に関する正本です。決定済みの仕様は [アーキテクチャ設計書](docs/architecture.md)、製品の目標・制約は [設計方針](docs/design-policy.md) に記載します。

## 1. 現在の到達点

- **設計段階**: [設計書](docs/architecture.md) に C4 モデル、識別・推定・保存・状態設計、重要シナリオ S1〜S9 を定義済みですが、[第4節の未決事項](#open-items) に依存するシナリオが残っており、初期設計は未完了です。
- **実装の退避**: 過去の実装・テスト・設定・データ等は [old/](old/README.md) に退避済みです。旧実装は単一 Hub の現在値受信・保存・認証付き表示までであり、現行の仕様とは異なります。
- **実機検証の状況**: 設計の再整理後、実機テストは未実施です。確認済みの根拠と未検証範囲は [設計書 第9節](docs/architecture.md#quality-and-risks) および [U1](#u1) で管理します。

## 2. 作業の進め方と順序

| 順序 | 作業内容 | 完了条件 |
| :---: | --- | --- |
| 1 | U2・U4 の契約帰属と利用枠の識別規則を、API 仕様と具体例から整理する | 同一・別・識別不能の判定基準が確定し、推定可能な範囲が明確になること |
| 2 | U3・U5 の比較基準・結果保存、U6・U9 の並行制御・停止・保存障害処理を重要シナリオへ反映する | 各シナリオの更新担当、コミット対象、異常発生時の保持状態が明確になること |
| 3 | U7・U8 の Hub 管理と手動更新手順を具体化し、認証・保存・復旧境界を整合させる | 管理操作および更新の正常系・異常系シナリオに検証条件が定まること |
| 4 | 設計が確定した機能から順次実装・検証を進め、U1 の実機確認を行う | 設計方針の受け入れ条件を満たし、必要な実機動作が確認できること |

※ 本順序は論理的な依存関係を示すものです。契約帰属の調査中でも、履歴日付の扱いや管理境界など独立した検討は並行して進めます。確定した仕様は設計書へ反映し、未実装機能は [第5節](#unimplemented) で追跡します。  
※ モック駆動開発の既定採用は U10、補助ツールの採否は U11 で個別に判断します。

## 3. 完了の判定

- **初期設計の完了**: S1〜S9 の各シナリオについて [文書方針の5項目](docs/document-policy.md#initial-design-completion) を具体的に説明できた時点とします。
- **製品初期版の完了**: 実装および必要な検証が [設計方針の受け入れ条件](docs/design-policy.md#product-completion) を満たした時点とします。  
（Linux 対応および端末の手動関連付けは、初期版完了後の後続スコープとします）


<a id="open-items"></a>
## 4. 未決事項・未検証事項

未決・未検証の課題は以下の ID で管理し、各項目の解消条件が満たされるまで追跡します。

| ID | 状態 | 依存・影響範囲 |
| --- | :---: | --- |
| [U1](#u1) 実 Hub と対応環境 | 未検証 | API 互換性、継続受信・再接続、Windows 実機検証（Linux は後続） |
| [U2](#u2) 利用額の契約帰属 | 未決・要調査 | S1・S4（契約別推定）。現在値表示および契約非依存の履歴収集とは独立 |
| [U3](#u3) 比較基準と推定の確定 | 未決 | U2・U4・U5 に依存。S1・S3・S4・S7（推定・再起動） |
| [U4](#u4) 利用枠の同一性 | 未決・要調査 | S1・S4（推定および契約・枠別履歴）。観測単位の保存・表示とは独立 |
| [U5](#u5) 端末別の比較基準 | 未決 | U2・U3・U4 に依存。S1・S4・S7（基準保持と端末分離） |
| [U6](#u6) 履歴・並行制御・収集停止 | 未決 | S2・S5・S7。保存異常処理は S6・U9 にも影響 |
| [U7](#u7) Hub 管理と認証境界 | 未決 | U6 の停止・再開に依存。S5・S8（秘密情報保護・CSRF 対策） |
| [U8](#u8) 手動更新と復旧 | 未決 | S9（互換性確認・履歴保全）。データ消失リスク等は事前合意を要する |
| [U9](#u9) 保存障害の確定点と影響範囲 | 未決・要検証 | S6・S7 および管理操作。共有 DB 障害時の他 Hub への影響分離 |
| [U10](#u10) モック駆動開発の既定採用 | 未決 | UI 変更フローと合意記録。内部ロジックや既存仕様の修正とは独立 |
| [U11](#u11) モック開発の補助製品 | 未決・要確認 | 対象 UI・通信境界と開発構成。プロセスの採否とは別に判断 |

<a id="u1"></a>
### U1. 実 Hub と対応環境の確認

**背景と現状:** 上流コミット `2f60827e` のソース調査および [Private Hub 単発取得ログ](old/docs/reference/hub-private/README.md) に基づく。旧実装では Windows 上での初回 SSE 受信と SQLite 保存まで確認済みだが、長時間の継続動作や現行仕様での検証は未実施である。

- [ ] 実 Hub に対する継続的な更新受信、切断・自動再接続、および長時間稼働を実機で検証する。
- [ ] 調査基準コミットと現在の上流（`c4182fd5`）との差分を精査し、API・識別項目・履歴・利用枠の互換性を確認する。
- [ ] 複数 Hub 連携、履歴収集、利用許容量の推定、管理操作、手動更新の実装後、Windows 実機で受け入れ条件を検証する。
- [ ] （後続スコープ）Linux 環境での動作確認および OS 間差異の検証。

**解消条件:** 実機環境（接続先 Hub の実装・リビジョン、実行環境）での検証シナリオと結果が記録され、未検証範囲が明確になっていること（Mock Hub による確認のみをもって代替としない）。

<a id="u2"></a>
### U2. 利用額を契約・利用枠へ帰属させる根拠

**確定事項:** Hub からの実績データは端末・ツール別の累積 API 換算額を含むが、`accountKey` や契約 ID は保持しない。そのため、単一アカウントの表示や名称の一致のみを理由にした機械的な結合は行わない。帰属が特定できない場合は「推定不可」として扱い、UI 上は実際の集計単位を明記して表示する。

- [ ] 採用する実績項目と集計期間を特定し、利用額の増分が特定契約の対象利用枠の消費に正しく対応する論理的根拠を確立する。
- [ ] 同一ツールでの複数契約・複数端末利用、契約・組織の変更、モデル限定枠の存在など、各種ケースにおける対応可能性と限界を整理する。

**解消条件:** API 項目に基づいて推定可能なケースと不可能なケースの具体例が明確化されていること。上流データのみで帰属が確定できない場合は不足情報を特定し、必要に応じて仕様調整を提示すること。

<a id="u3"></a>
### U3. 比較基準・観測・推定結果の管理

**前提:** [設計書 第5節](docs/architecture.md#crosscutting) に従い、同一 SSE 内の利用額と消費率をペアとし、同一対象期間内の比較可能な最初と最新の観測から推定を算出する。結果は算出根拠とともに追記保存し、照合・計算・保存は単一キューで直列化して保存完了後に通知する。

- [ ] 契約帰属および枠識別の成立条件に合わせ、観測データの同一性判定、順序管理、および最初・最新の比較基準を更新する責務を定義する。
- [ ] 観測、比較基準、推定結果、および推定不可理由を一括永続化するトランザクション範囲と、同一通知の再受信による重複算出を防ぐ規則を策定する。
- [ ] 切断に伴う欠測、消費率の減少、古いデータの再受信、リセット予定時刻の更新が発生した際の、比較再開ルールを明確化する（不明区間を跨ぐ計算は行わない）。
- [ ] 同一利用枠を複数端末が報告する場合の利用額と代表消費率の対応、端末構成変化時の挙動、再起動後の継続処理を具体化する。

**解消条件:** 通常受信、再受信、リセット、欠測、保存失敗、再起動の各ケースについて、採用する観測、更新担当、コミット対象、保持される基準・結果が定義され、検証条件が定まっていること。

<a id="u4"></a>
### U4. 利用枠を観測間で識別する規則

**前提:** 利用枠自体の同一性と、その対象期間（リセット周期）は独立して判定する。Claude では同一 `kind` 内のモデル別枠、Codex では同一 `limitId` 内の期間別枠が存在する点に留意する。

- [ ] 各プロバイダーにおいて、アカウント情報、`kind`、`limitId`、`additional` などのどの組み合わせによって同一枠と判定できるかを特定する。
- [ ] 名称、プラン、組織、任意フィールドの変更や配列順の変動が生じた場合に、同一枠として継続できるケースと別枠扱いになるケースを定義する。
- [ ] 識別不能な枠の保存・表示単位と、後続の観測で識別可能になった場合の扱いを定める（過去の推定結果は遡及変更しない）。

**解消条件:** 単独項目では衝突する具体例を含め、同一・別・識別不能の判定基準が整理されていること（`resetsAt` は枠識別後の対象期間判定に使用する）。

<a id="u5"></a>
### U5. 端末変更と比較基準の分離

**確定事項:** Hub と `deviceId` の組み合わせで端末を識別し、ID 変更時は別端末として扱う（自動名寄せは行わない）。将来的な手動関連付け機能は初期版の対象外とする。

- [ ] 端末ごとの比較基準保持と、単一の契約・利用枠を複数端末で利用する場合のデータ構造・保存モデルを定義する。
- [ ] 旧端末の停止と新端末の追加が発生した際に、利用額の二重計上や別端末データの誤った継続処理を防止する条件を策定する。

**解消条件:** 同一 ID の異なる Hub、同一 Hub の異なる端末、および ID 変更の各パターンにおいて、保存データと比較基準が正しく分離されることが説明できること。

<a id="u6"></a>
### U6. 履歴取得・日付・収集停止の制御

**前提:** [設計書（S2・S5）](docs/architecture.md#runtime) に基づき、通信の独立実行と受信後の直列化、毎日午前1時の定期取得、失敗時の1時間間隔再試行、端末現地日付の保持、停止受付前の受信完了データのコミットを定めている。

- [ ] 端末のタイムゾーン情報が欠落または不正な場合に、日次データの当日分除外を安全に判定するルールと、判定不能時の保存・表示・継続範囲を決定する。
- [ ] 初回取得、再接続、定期取得、再試行が競合した場合のリクエスト制御、キャンセル処理、古い応答の採用可否を整理する。
- [ ] 履歴レコード、取得完了フラグ、最終成功日時の保存担当コンポーネントおよびトランザクション境界を定める（正常取得後の前日データ不在と通信失敗の区別を含む）。
- [ ] 停止要求の受付、通信キャンセル、受信済みキューの処理完了、停止フラグの保存、画面通知の一連のシーケンスと担当を明確化する。
- [ ] 停止フラグ保存失敗時や処理途中の再起動、停止・再開が連続した場合の状態復元および再接続ルールを策定する。

**解消条件:** 履歴取得中の新規 SSE 受信、停止前後の応答処理、日付境界、保存失敗、再起動の各ケースについて、採用データ、担当、コミット対象、維持される状態、継続・停止する処理が明確になっていること。

<a id="u7"></a>
### U7. Hub 管理と認証・秘密情報

**確定事項:** Web 画面から Hub を登録・一覧表示し、個別に収集停止・再開を行えるようにする。全エンドポイントに Basic 認証を適用し、更新操作には CSRF 対策を施す。

- [ ] Hub の新規登録、接続先変更、秘密情報更新における API 契約、入力バリデーション、および結果表示を定義する。
- [ ] 接続設定および秘密情報の安全な永続化方法を定める（ログや画面レスポンスへの漏洩防止を含む）。
- [ ] 管理 API における CSRF 対策の実装方式と、操作要求の受付・実行・通知境界を明確化する。

**解消条件:** 正常操作、認証エラー、CSRF 検証失敗、入力値不正、保存失敗の各状況において、安全性が担保される仕様が確定していること。

<a id="u8"></a>
### U8. 手動更新と履歴を保全する復旧

**確定事項:** ユーザーの明示操作によって実行し、処理中のサービス一時停止を許容する。自動ロールバックや DB 再作成は行わず、失敗原因の記録と手動復旧情報の提示を行う。

- [ ] 更新元・更新対象の範囲、取得・適用・再起動のシーケンスと実行主体を決定する。
- [ ] アプリケーションと保存データの互換性チェック、更新失敗時に残存するファイル・データの整合性、およびユーザーによる手動復旧手順を策定する。
- [ ] 更新操作の認証・CSRF 対策、障害ログの出力、および再起動後の状態表示を具体化する。

**解消条件:** 更新処理が中断・失敗した場合でも、既存履歴を保護したまま復旧できる手順と検証条件が確立されていること。

<a id="u9"></a>
### U9. 保存障害の確定点と影響範囲

**背景:** 旧実装ではトランザクションコミット後に現在値の再読み出しを行っており、コミット前の書き込み失敗と、コミット完了後の読み出し・通知失敗では状態が異なる。

- [ ] コミット完了後の読み出しや通知で障害が発生した場合の、永続化済みデータ、UI 保持値、配信状態、再起動時の復元動作を定義する。
- [ ] DB 接続・読み出し自体が不能となった場合の、フォールバック表示および停止機能の範囲を明確化する。
- [ ] 複数 Hub で共有する単一 SQLite において異常が発生した際、書き込み停止の範囲と、継続可能な通信・閲覧・状態通知の境界を整理する。

**解消条件:** コミット前の失敗、コミット後の失敗、DB アクセス不能の各ケースについて、永続化状態と UI 表示状態の整合性が検証可能に整理されていること。

<a id="u10"></a>
### U10. モック駆動開発を既定の進め方にするか

**背景:** [ADR 0002](docs/ard/0002-adopt-mock-driven-development.md) および [標準規約案](docs/standards/mock-driven-development.md) に基づく検討項目。

- [ ] 実際の UI 操作による認識合わせを必須とする変更の範囲と、モック駆動開発を既定プロセスとして採用するかを判断する。

**解消条件:** 採用の可否および適用範囲について方針を決定し、文書方針および ADR に反映すること。

<a id="u11"></a>
### U11. モック開発の補助製品と配置

**背景:** [ADR 0003](docs/ard/0003-select-mock-tooling.md) および [ツール候補](docs/mock-tooling.md) に基づく検討項目。

- [ ] 対象 UI、通信プロトコル（JSON / SSE）の再現範囲、MSW と同梱 Mock Hub の役割分担を整理する。
- [ ] 代表画面を用いて、Storybook、MSW、Playwright の組み合わせによる開発・保守負担と効果を評価し、導入構成（フルセットまたは最小構成）を判断する。

**解消条件:** 採用ツールの選定理由と構成を確定し、導入する場合はディレクトリ配置・実行コマンド・モック接続と実機接続の切り替え手順を策定すること。

<a id="unimplemented"></a>
## 5. 確定済みだが未実装の機能

以下の機能は仕様要件として確定しており、設計・実装フェーズで順次対応します（必要な設計上の論点は第4節の各 ID で管理）。

- [ ] 契約・利用枠別の利用許容量推定、比較基準・算出根拠の保存、推定履歴の閲覧機能
- [ ] 日次・月次利用実績の GET 収集、過去データの補完、推移保存および比較機能
- [ ] 複数 Hub の並行収集、管理画面（登録・一覧・個別停止・再開・設定復元）
- [ ] 管理操作における CSRF 対策
- [ ] 手動アップデート機能および失敗時の復旧支援機能・運用手順

※ 各機能の実装後は [設計書の S1〜S9](docs/architecture.md#runtime) および設計方針の受け入れ条件に基づき検証を実施します。Linux 対応と端末の手動関連付けは、初期版完了後の後続範囲として扱います。
